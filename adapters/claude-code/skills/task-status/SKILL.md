---
name: task-status
description: Показывает сводку по задачам конвейера — прогресс этапов, ветки, артефакты. Запускать, когда пользователь хочет увидеть состояние задач (всех или одной по TASK-ID). Не трогает внешние репозитории, работает без заполненного .env. Имя task-status выбрано вместо status (status занят встроенной командой).
---

Этап конвейера conveyor: сводка по задачам.
**Агент:** не требуется. Внешние репозитории не трогает.

**Корень плагина.** `${CLAUDE_PLUGIN_ROOT}` — Claude Code подставил сюда
абсолютный путь при загрузке скилла. Ниже и во всех файлах этапа он обозначен
плейсхолдером `<CONVEYOR_ROOT>`: подставляй этот путь БУКВАЛЬНО в каждую
команду и каждый путь. Переменной окружения с таким именем нет — `$` с
фигурными скобками раскроется в пустоту, а окружение между вызовами Bash не
сохраняется. Остался незаменённый текст вместо пути — возьми каталог из строки
«Base directory for this skill» выше и поднимись на два уровня.

Прочитай и выполни точно: `<CONVEYOR_ROOT>/core/stages/task-status.md`.

Кратко:
1. Найди рабочий репозиторий (каталог с settings.json, поднимаясь вверх).
2. Прочитай tasks/FE/*/meta.json и tasks/BE/*/meta.json.
3. Без TASK-ID — таблица по всем: TASK-ID, тип, title, прогресс (N/6),
   следующий этап, ветки, связанная задача. С TASK-ID — детали одной задачи.
4. Повреждённый meta.json — пометь и продолжай. Ничего не меняй.
