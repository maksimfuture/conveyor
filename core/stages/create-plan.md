# Этап /create-plan — план реализации

**Кто запускает:** разработчик. **Агент и кодовая база** — по типу задачи:
FE → frontend-developer + `repos.frontend.link`; BE → backend-developer +
`repos.backend.link` (путь берётся из settings.json через resolve-config, не
спрашивается и не угадывается).
**Аргументы:** `TASK-ID`. **Предусловие:** существует `specification.md`.

**Рабочая область:** чтение — ТОЛЬКО кодовая база по типу задачи
(repos.frontend или repos.backend) + specification.md; запись — ТОЛЬКО
артефакты задачи (plan.md, meta.json). Репозиторий анализа, автотестов и
чужая кодовая база не открываются и агенту не сообщаются.

## Алгоритм
1. Определи тип задачи из meta.json → выбери агента и репозиторий кода.
   Установи рабочую область: `scope.mjs set --stage create-plan --type
   <FE|BE> --task <TASK-ID>` (запись в репозитории запрещена).
2. Найди рабочую копию кода (`git-ops locate`) и обнови (`update --mode read`).
3. Запусти агента-разработчика: вход — specification.md + путь к коду. Он
   обязан изучить архитектуру и соглашения проекта и составить `plan.md` по
   `core/templates/plan.md`.
4. Валидация (скилл):
   `node "${CONVEYOR_ROOT}/core/scripts/validate-artifact.mjs" --file <папка задачи>/plan.md --type plan`
   плюс покрытие: каждый шаг привязан к файлам, есть раздел рисков, каждый
   REQ-ID спецификации покрыт хотя бы одним шагом либо явно указано, почему
   требование вне плана. Не сошлось — один автоматический возврат агенту,
   затем покажи пользователю остаток.
   Возврат делай не только при `ok:false`, но и при непустом `placeholders`:
   в артефакте остался каркас шаблона, а цикла ревью на этом этапе нет и
   другого сигнала «сюда не написали» не будет.
5. `validate-task-folder` (см. `_common.md`, «Завершение этапа»), затем
   обнови meta.json (`stages.plan.done = true`). Сними рабочую область
   (`scope.mjs clear`).

## Ошибки
- кодовая база недоступна → остановка с инструкцией (см. `_common.md`,
  «Работа с внешними репозиториями»);
- валидация покрытия REQ-ID не прошла → один автоматический возврат агенту со
  списком непокрытых REQ-ID; после повторной неудачи — покажи пользователю,
  что осталось непокрытым.

## DoD
План прошёл валидацию покрытия REQ-ID; пользователь его видел. Следующий шаг
— `/implement-plan`.
