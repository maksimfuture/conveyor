# План автотестов: <название> (<TASK-ID>)

<!-- Артефакт этапа /conveyor:create-autotest-plan. Заполняет агент
     qa-autotest-engineer по specification.md. Кодовая база продукта не
     открывается: тесты проверяют СПЕЦИФИКАЦИЮ, а не то, что получилось в
     коде. Репозиторий автотестов при этом читается — из него берутся
     соглашения и существующие классы, которые попадают в колонку «Класс».
     Путь к классу в плане не делает план «планом по коду»: ЧТО проверять
     взято из спецификации, из репозитория взято только ГДЕ это писать.
     Каждый критерий приёмки спецификации должен быть покрыт кейсом либо
     помечен «не автоматизируем» с причиной.

     Всё, что можно положить в таблицу, лежит в таблице: отдельных описаний
     кейсов под таблицей нет — шаги и ожидаемый результат живут в самой
     строке. Кейсы и автотесты — две таблицы об одних и тех же тестах: кейс
     говорит, ЧТО проверяем, автотест — в каком классе это писать. Связывает
     их ID.

     ID — идентификатор теста в Jira (аннотация `@TmsLink` в коде теста).
     Существующий тест — его реальный ID (например `PROJ-T113`); нового в TMS
     ещё нет — плейсхолдер из ключа проекта, `T999` и сквозного номера по
     плану: `PROJ-T999-1`, `PROJ-T999-2`. Одинаковый ID у двух новых тестов
     недопустим: по СОСТАВУ ID следующий этап сверяет отчёт о прогоне с
     планом, и потерянный тест иначе не поймать.

     Форма: таблицы и списки, а не абзацы; строка — один факт. Один и тот же
     факт не повторяется в двух разделах. «Итого» пишется ПОСЛЕДНИМ и только
     по фактам из разделов выше — новых утверждений в нём нет.

     Колонку «Статус» таблицы автотестов заполняет следующий этап,
     /conveyor:implement-auto-test; здесь она остаётся `[ ]`. -->

## Объект тестирования
- <что проверяем по спецификации, в границах задачи>

## Уровни тестирования
<!-- ТОЛЬКО уровни, на которых будут написаны тесты. Отклонённые уровни не
     перечисляются: раздел показывает состав работ, а не защищает выбор. -->
- **<уровень>** — <почему тесты этого уровня нужны, одна строка>

## Тест-кейсы
<!-- ВСЕ кейсы одной таблицей, включая те, что останутся на ручном прогоне:
     отдельных описаний под таблицей нет, шаги и ожидаемый результат — в
     строке. Переводов строки в ячейке не бывает: шаги пишутся подряд —
     `1) ... 2) ...`; вертикальная черта внутри ячейки экранируется как `\|`,
     иначе строка разъедется на лишнюю колонку. В первой ячейке — только ID.
     Угловые скобки в тексте ячейки не используй: пиши `нажать Enter` и
     `List(OrderDto)` — текст в угловых скобках детектор читает как
     незаполненное место каркаса и вернёт готовый план на доработку.
     «Вид тестирования»: `НФ` — проверка нового поведения,
     `Регресс` — защита уже работающего.

     «Автотест» — `да` либо `нет`: покрытие видно прямо в таблице, не сверяя
     её построчно с таблицей автотестов. Колонка не украшение, а вторая
     запись того же факта, и валидатор сверяет её с остальным планом: `да`
     требует строки с этим ID в «Автотестах», `нет` — отсутствия такой строки
     и упоминания кейса в «Что не автоматизируем». Так «решили не
     автоматизировать» не спутать с «потеряли». -->

| ID | Вид тестирования | Уровень | Название | Шаги | Ожидаемый результат | REQ-ID | Автотест |
|---|---|---|---|---|---|---|---|
| PROJ-T999-1 | НФ | <уровень> | <кратко, одной строкой> | 1) <шаг> 2) <шаг> | <что ожидаем> | REQ-1 | да |
| PROJ-T113 | Регресс | <уровень> | <кратко, одной строкой> | 1) <шаг> | <что ожидаем> | REQ-2 | да |
| PROJ-T999-2 | НФ | <уровень> | <кратко, одной строкой> | 1) <шаг> | <что ожидаем> | REQ-3 | нет |

## Автотесты
<!-- Что будет написано кодом: строка на каждый тест из таблицы кейсов,
     который автоматизируется, с тем же ID. «Класс» — файл, куда тест ляжет;
     «Реализация» — создать класс / изменить (какие методы добавить) /
     переиспользуется без правок (только прогон). Столько строк, сколько
     будет строк в таблице report-auto-test.md — по составу ID их сверяет
     /conveyor:implement-auto-test. «Статус» на этом этапе — `[ ]` у всех.
     Регресс берут выборочно, а не «весь набор целиком»: строка с
     обоснованием под таблицей обязательна, если в ней есть хоть один
     регрессный тест. -->

| ID | Вид тестирования | Уровень | Класс | Реализация | REQ-ID | Статус |
|---|---|---|---|---|---|---|
| PROJ-T999-1 | НФ | <уровень> | `<путь к классу>` | создать: <что именно> | REQ-1 | [ ] |
| PROJ-T113 | Регресс | <уровень> | `<путь к классу>` | переиспользуется без правок: только прогон | REQ-2 | [ ] |
- **Регресс: почему выбраны эти тесты** — <что именно они защищают от поломки>

## Тестовые данные и окружение
- **Стенд:** <окружение и ключ конфигурации>
- **Данные и фикстуры:** <файлы, учётные записи, сертификаты>
- **Предусловия окружения:** <что выполнено до прогона>

## Итого
<!-- Пишется ПОСЛЕДНИМ и только по фактам из разделов выше: счётчики берутся
     из таблиц, а не из головы. Счётчик автотестов — то же число, что
     /conveyor:implement-auto-test сверит с отчётом о прогоне. -->
- **Уровни:** <какие уровни затронуты>
- **Кейсы:** <всего> — покрыто автотестами <N>, без автоматизации <N>
- **Автотесты:** <всего> — НФ <N>, регресс <N>
- **Файлов затронуто:** <N> — создать <N>, изменить <N>, без правок <N>
- **Тестовые данные и фикстуры:** <меняются: что / не меняются>
- **Не автоматизируем:** <N> пунктов

## Что не автоматизируем и почему
<!-- Каждый кейс без строки в «Автотестах» назван здесь своим ID — по нему
     видно, что автоматизации не будет осознанно. Шаги ручного прогона живут
     в таблице кейсов, здесь — только причина. -->
- **<пункт> (REQ-3)** — <причина, одна строка>. Кейсы: PROJ-T999-2

## Приложение: решения и координация
<!-- Необязательный раздел, валидатор его не требует. Сюда — принятые по ходу
     решения, закрытые вопросы и договорённости с командой: в основной части
     плана они рвут чтение. Нечего писать — удали раздел целиком. -->
