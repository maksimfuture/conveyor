# План автотестов: <название> (<TASK-ID>)

<!-- Артефакт этапа /conveyor:create-autotest-plan. Заполняет агент
     qa-autotest-engineer по specification.md. Кодовая база продукта не
     открывается: тесты проверяют СПЕЦИФИКАЦИЮ, а не то, что получилось в
     коде. Репозиторий автотестов при этом читается — из него берутся файлы,
     существующие тесты и фикстуры для разделов «Что переиспользуем» и
     «Шаги реализации тестов». Каждый критерий приёмки спецификации должен
     быть покрыт кейсом либо помечен «не автоматизируем» с причиной.

     Форма: списками, а не абзацами; строка — один факт; обоснования не
     живут в таблице кейсов. Один и тот же факт не повторяется в двух
     разделах. «Итого» пишется ПОСЛЕДНИМ и только по фактам из разделов
     ниже — новых утверждений в нём нет.

     Чекбоксы «Шагов реализации тестов» отмечает следующий этап,
     /conveyor:implement-auto-test; здесь они остаются пустыми. -->

## Итого
- **Уровни:** <какие уровни затронуты>
- **Кейсы:** <всего> — новых <N>, переиспользуется <N>
- **Файлов затронуто:** <N> — создать <N>, изменить <N>, без правок <N>
- **Тестовые данные и фикстуры:** <меняются: что / не меняются>
- **Не автоматизируем:** <N> пунктов

## Уровни тестирования
- **Выбрано:** <уровень> — <почему, одна строка>
- **Отклонено:** <уровень> — <почему не нужен>

## Объект тестирования
- <что проверяем по спецификации, в границах задачи>

## Что переиспользуем
- `<класс, хелпер или фикстура>` — <как используем>

## Шаги реализации тестов
- [ ] **Шаг 1.** Файл(ы): `<путь>` — <создать или изменить: что именно>. Кейсы: TC-1, TC-2. Закрывает: REQ-1.
- [ ] **Шаг 2.** Файл(ы): `<путь>` — <переиспользуется без правок: только прогон>. Кейсы: TC-3. Закрывает: REQ-2.

## Тест-кейсы
| ID | Название | Уровень | Действие | Приоритет | REQ-ID |
|---|---|---|---|---|---|
| TC-1 | <кратко, одной строкой> | <уровень> | новый | P1 | REQ-1 |
| TC-3 | <кратко, одной строкой> | <уровень> | переиспользуется: `<тест>` | P2 | REQ-2 |

<!-- Детальный блок ниже — ТОЛЬКО для новых кейсов. У переиспользуемого
     кейса описание уже есть в самом тесте: ссылки из таблицы достаточно,
     шаги дублировать не нужно. -->

### TC-1 — <название кейса>
- **Предусловия:** <по строке на пункт>
- **Шаги:**
  1. <шаг>
  2. <шаг>
- **Ожидаемый результат:** <по строке на проверку>

## Тестовые данные и окружение
- **Стенд:** <окружение и ключ конфигурации>
- **Данные и фикстуры:** <файлы, учётные записи, сертификаты>
- **Предусловия окружения:** <что выполнено до прогона>

## Что не автоматизируем и почему
- **<пункт> (REQ-N)** — <причина, одна строка>

## Приложение: решения и координация
<!-- Необязательный раздел, валидатор его не требует. Сюда — принятые по ходу
     решения, закрытые вопросы и договорённости с командой: в основной части
     плана они рвут чтение. Нечего писать — удали раздел целиком. -->
