# План автотестов: <название> (<TASK-ID>)

<!-- Артефакт этапа /conveyor:create-autotest-plan. Заполняет агент
     qa-autotest-engineer по specification.md. Кодовая база продукта не
     открывается: тесты проверяют СПЕЦИФИКАЦИЮ, а не то, что получилось в
     коде. Репозиторий автотестов при этом читается — из него берутся файлы,
     существующие тесты и фикстуры для разделов «Что переиспользуем» и
     «Шаги реализации тестов». Каждый критерий приёмки спецификации должен
     быть покрыт кейсом либо помечен «не автоматизируем» с причиной.

     Тест-кейсы и автотесты — РАЗНЫЕ разделы, но кейсы на «ручные» и
     «автоматизированные» НЕ делятся: все они в одной таблице, а колонка
     «Автотест» говорит, каким `AT-N` кейс закрыт (или «нет»). Раздел
     «Автотесты» описывает, что будет написано кодом, и делится на новый
     функционал и регресс. Число автотестов из этого раздела — то же число,
     что попадёт в report-auto-test.md.

     Форма: списками, а не абзацами; строка — один факт; обоснования не
     живут в таблице кейсов. Один и тот же факт не повторяется в двух
     разделах. «Итого» пишется ПОСЛЕДНИМ и только по фактам из разделов
     ниже — новых утверждений в нём нет.

     Чекбоксы «Шагов реализации тестов» отмечает следующий этап,
     /conveyor:implement-auto-test; здесь они остаются пустыми. -->

## Итого
- **Уровни:** <какие уровни затронуты>
- **Кейсы:** <всего> — покрыто автотестами <N>, без автоматизации <N>
- **Автотесты:** <всего> — новый функционал <N>, регресс <N>
- **Файлов затронуто:** <N> — создать <N>, изменить <N>, без правок <N>
- **Тестовые данные и фикстуры:** <меняются: что / не меняются>
- **Не автоматизируем:** <N> пунктов

## Уровни тестирования
- **Выбрано:** <уровень> — <почему, одна строка>
- **Отклонено:** <уровень> — <почему не нужен>

## Объект тестирования
- <что проверяем по спецификации, в границах задачи>

## Что переиспользуем
- `<класс, хелпер или фикстура>` — <как используем>

## Шаги реализации тестов
- [ ] **Шаг 1.** Файл(ы): `<путь>` — <создать или изменить: что именно>. Автотесты: AT-1. Кейсы: TC-1, TC-2. Закрывает: REQ-1.
- [ ] **Шаг 2.** Файл(ы): `<путь>` — <переиспользуется без правок: только прогон>. Автотесты: AT-2. Кейсы: TC-3. Закрывает: REQ-2.

## Тест-кейсы
<!-- ВСЕ кейсы одной таблицей: что делаем и что ожидаем, текстом. Кейсы не
     делятся на ручные и автоматизированные — за это отвечает колонка
     «Автотест»: ID автотеста, который его закрывает, либо «нет» (тогда
     причина живёт в «Что не автоматизируем»). Что именно делает автотест —
     ниже, в разделе «Автотесты»; здесь только ссылка. -->

| ID | Название | Уровень | Приоритет | Автотест | REQ-ID |
|---|---|---|---|---|---|
| TC-1 | <кратко, одной строкой> | <уровень> | P1 | AT-1 | REQ-1 |
| TC-3 | <кратко, одной строкой> | <уровень> | P2 | AT-2 | REQ-2 |
| TC-4 | <кратко, одной строкой> | <уровень> | P3 | нет | REQ-2 |

<!-- Детальный блок ниже нужен двум видам кейсов: НОВЫМ (по нему пишут
     автотест) и тем, у кого в колонке «Автотест» стоит «нет» (по нему кейс
     проходят руками — больше его описать негде: в «Что не автоматизируем»
     лежит причина, а не шаги). Кейс, закрытый уже существующим автотестом
     (в «Шагах реализации» — «переиспользуется без правок»), описан в самом
     тесте: строки таблицы достаточно. -->

### TC-1 — <название кейса>
- **Предусловия:** <по строке на пункт>
- **Шаги:**
  1. <шаг>
  2. <шаг>
- **Ожидаемый результат:** <по строке на проверку>

### TC-4 — <название кейса>
<!-- Кейс с «нет» в колонке «Автотест»: автоматизации не будет, поэтому шаги
     и ожидаемый результат нужны здесь — иначе проходить его не по чему. -->
- **Предусловия:** <по строке на пункт>
- **Шаги:**
  1. <шаг>
- **Ожидаемый результат:** <по строке на проверку>

## Автотесты
<!-- Что будет написано кодом. Каждому автотесту — ID `AT-N`; какой кейс он
     закрывает, видно из колонки «Автотест» таблицы выше, здесь это не
     дублируется. Новый функционал сгруппирован по УРОВНЯМ тестирования
     (заголовок `#### Уровень: <уровень>` на группу), регресс описан отдельным
     подразделом. Автотестов здесь ровно столько, сколько строк будет в отчёте
     о прогоне. -->

### Новый функционал

#### Уровень: <уровень>
- **AT-1. <название теста>**
  - **Код теста:** <что именно делает тест: вызовы, действия, проверки>
  - **Описание:** <что происходит и зачем этот тест написан>

### Регресс
- **Уровень:** <уровень> — <почему регресс сделан именно на этом уровне>
- **Список:**
  - **AT-2. <название теста>** — `<тест>` — <что защищает от поломки>
- **Почему выбраны эти тесты:** <по строке на причину>

## Тестовые данные и окружение
- **Стенд:** <окружение и ключ конфигурации>
- **Данные и фикстуры:** <файлы, учётные записи, сертификаты>
- **Предусловия окружения:** <что выполнено до прогона>

## Что не автоматизируем и почему
- **<пункт> (REQ-N)** — <причина, одна строка>

## Приложение: решения и координация
<!-- Необязательный раздел, валидатор его не требует. Сюда — принятые по ходу
     решения, закрытые вопросы и договорённости с командой: в основной части
     плана они рвут чтение. Нечего писать — удали раздел целиком. -->
