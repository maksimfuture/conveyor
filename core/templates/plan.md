# План реализации: <название> (<TASK-ID>)

<!-- Артефакт /create-plan (агент-разработчик). Чекбоксы и раздел
     «Отклонения и остатки» заполняются на этапе /implement-plan. -->

## Краткое резюме подхода
<в двух-трёх предложениях: как решаем>

## Затронутые репозитории

<!-- Строка на КАЖДУЮ рабочую копию, которую придётся ПРАВИТЬ. Идентификатор —
     как в settings.json: frontend, backend.core, backend.api, backend.common,
     backend.config. Репозитории, которые только читаем, сюда не попадают.
     Этот список — рабочая область /implement-plan: в репозиторий, которого
     здесь нет, guard писать не даст. Он же обязан совпасть с тегами
     «Репозиторий:» у шагов — расхождение валидатор считает ошибкой.
     Вместо `<id>` подставь настоящие идентификаторы, строк добавь столько,
     сколько репозиториев правишь. У FE-задачи рабочая копия одна — строка
     тоже одна. Настоящих идентификаторов в каркасе НЕТ намеренно: иначе
     нетронутый шаблон читался бы как заполненный план и открыл бы запись в
     репозитории, которых ты не выбирал. -->

| Репозиторий | Что меняем | Почему здесь | Порядок мержа |
|---|---|---|---|
| `<id>` | <что меняем> | <почему именно этот репозиторий> | 1 |
| `<id>` | <что меняем> | <почему именно этот репозиторий> | 2 |

**Зависимости между репозиториями:** <как правка в одном доезжает до другого —
версия артефакта, snapshot, пересборка; либо «нет зависимостей».>

## Анализ текущего кода
<Что переиспользуем, ключевые модули/файлы — по каждому затронутому репозиторию.>

## Шаги реализации
- [ ] **Шаг 1.** Репозиторий: `<id>`. Файл(ы): `<путь>` — <что сделать>. Закрывает: REQ-1, REQ-2.
- [ ] **Шаг 2.** Репозиторий: `<id>`. Файл(ы): `<путь>` — <что сделать>. Закрывает: REQ-3.

## Изменения контрактов/схем
<Эндпоинты, схемы, миграции — с указанием репозитория; отдельно то, что ломает
соседний репозиторий, и как это смягчаем. Или «нет».>

## Риски и альтернативы
- <риск → как смягчаем>

## Стратегия тестирования
<Unit/интеграционные тесты внутри кодовой базы — по каждому затронутому репозиторию.>

## Что вне скоупа
- <что осознанно не делаем в этой задаче>

## Отклонения и остатки
<!-- Пусто на этапе плана. Заполняет /implement-plan: что пошло не по плану,
     что осталось нерешённым, результаты линтеров/тестов по репозиториям,
     добавленные по ходу репозитории. -->

## Ревью
<!-- Заполняется циклом ревью (_review-loop.md) на этапе /implement-plan.
     На /create-plan раздел ОСТАЁТСЯ заглушкой: ревью там не запускается,
     и заполнить его нечем. Поэтому здесь нет полей в угловых скобках —
     иначе валидатор считал бы готовый план незаполненным и возвращал его
     агенту на каждом прогоне. Формат, который пишет /implement-plan:
       - **Раундов:** N из reviewRounds
       - **Исправлено:** краткий список устранённых замечаний
       - **Отклонено (REBUT → снято):** finding — обоснование / «нет»
       - **Нерешённые (эскалированы пользователю):** blocker|major — позиции / «нет»
     При reviewRounds=0 — «ревью отключено (reviewRounds=0)». -->
— (заполняется на `/implement-plan`)
