# Отчёт по автотестам: <название> (<TASK-ID>)

<!-- Артефакт /implement-auto-test. Заполняет qa-autotest-engineer СРАЗУ
     ПОСЛЕ ЦИКЛА РЕВЬЮ — до push и до сборки в CI. На этот момент известно
     всё, кроме прогона в CI: раздел «Прогон в CI» получает маркер «ожидается
     прогон в CI», и его дописывает скилл, когда сборка отработает.

     Количество автотестов в отчёте обязано совпадать с планом: сколько
     `AT-N` в разделе «Автотесты» autotest-plan.md, столько строк и в таблице
     ниже. Тест не написан или появился сверх плана — это строка «Расхождение
     с планом» с причиной, а не молча изменённое число. -->

## Итог прогона
- **Автотестов по плану:** <всего> — новый функционал <N>, регресс <N>
- **Реализовано:** <N>
- **Расхождение с планом:** <нет / AT-N не реализован: причина; AT-N сверх плана: причина>
- **Пройдено:** <N>
- **Упало:** <N>
- **Пропущено/заблокировано:** <N>

## Реализованные автотесты
| ID | Название | Уровень | Вид | Файл теста | Статус | Комментарий |
|---|---|---|---|---|---|---|
| AT-1 | <название теста> | <уровень> | новый функционал | `<путь>` | пройден / упал / пропущен | <причина падения: дефект продукта или теста> |
| AT-2 | <название теста> | <уровень> | регресс | `<путь>` | пройден / упал / пропущен | <причина падения: дефект продукта или теста> |

## Прогон в CI
<!-- Единственный раздел, который заполняет СКИЛЛ, а не агент: агент пишет
     отчёт сразу после ревью, когда сборки ещё не было, и ставит маркер
     «ожидается прогон в CI» в «Сборку» и «Статус». Дождавшись сборки, скилл
     правит эти две строки и «Тесты в этом прогоне» — точечно, не переписывая
     отчёт. Пока маркер на месте, отчёт промежуточный: этап не закрывается
     (validate-artifact вернёт ciPending: true).
     Джоба автотестов в Jenkins (settings.json → repos.autoTest.
     linkPipelineAutoTest). Ссылку на джобу пиши ВСЕГДА — она известна из
     настроек ещё до запуска, и по ней читатель откроет историю прогонов,
     даже если этой сборки не было. Прогон идёт ПО ТЕГАМ и потому шире задачи: в
     сборке участвуют все тесты с этими тегами, а не только AT-N. Числа отсюда
     НЕ переносятся в «Итог прогона» и в сверке отчёта с планом не участвуют.
     Статус — как его вернул Jenkins (`SUCCESS`, `UNSTABLE` — сборка прошла,
     но тесты падали, `FAILURE` — упала сама сборка, `ABORTED` — прервали;
     иное значение пиши как есть). «В процессе» — не статус Jenkins, а наш:
     ждать перестали, сборка ещё идёт.
     Сборки не было — раздел не удаляй и ссылку на джобу не стирай: причина
     идёт в «Статус» — «джоба с автотестами не запускалась: <почему>» (отказ
     пользователя, ветка не запушена, нет ссылки на джобу в settings.json,
     MCP-сервер недоступен). -->
- **Джоба:** <ссылка на джобу — всегда, даже если сборки не было>
- **Ветка:** <ветка, с которой собирали>
- **Теги:** <теги, с которыми запускали — после правки пользователем>
- **Сборка:** #<номер> — <ссылка на сборку> / «ожидается прогон в CI»
- **Статус:** <SUCCESS / UNSTABLE / FAILURE / ABORTED / в процессе> / «ожидается прогон в CI» / «джоба с автотестами не запускалась: <причина>»
- **Тесты в этом прогоне:** <по данным Jenkins: всего N, упало N, пропущено N>

## Блокировки
<Если окружение недоступно — что мешает запуску и что нужно для него.>

## Ревью
<!-- Заполняется циклом ревью (_review-loop.md), домен autotests.
     При reviewRounds=0 — «ревью отключено (reviewRounds=0)». -->
- **Раундов:** <N> из reviewRounds
- **Исправлено:** <краткий список замечаний, которые устранены>
- **Отклонено (REBUT → снято):** <finding — обоснование> / «нет»
- **Нерешённые (эскалированы пользователю):** <blocker|major — позиции> / «нет»

## Ветка
- **autotestBranch:** <TASK-ID>-autotests
