# Conveyor 2.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Перевести плагин conveyor на локальные рабочие копии в `repos/`, ввести этап бизнес-аналитика (intent), объединить `create-feature` и `create-specification` в один этап и построить план автотестов от спецификации — не сломав существующий рабочий репозиторий.

**Architecture:** Ядро (`core/`) остаётся платформенно-независимым, адаптеры (`adapters/claude-code`, `adapters/gigacode`) — тонкими. Ключевое упрощение: ссылка на репозиторий превращается из «локальный путь ИЛИ git-URL + кэш клонов» в «путь относительно корня рабочего репозитория», поэтому исчезают `repoCache`, `.cache/repos/`, клонирование в `git-ops locate` и все развилки по `isGitUrl`. Конвейер становится: intent (БА) → спецификация (СА, правит анализ и пишет спеку) → план → реализация → план автотестов → автотесты.

**Tech Stack:** Node.js ≥ 14 (ESM, только `node:fs`/`node:path`/`node:os`/`node:crypto`/`node:child_process`), markdown-стейджи и промпты, Claude Code plugin (skills + agents + hooks), GigaCode extension (commands).

**Тестовый контур:** `node scripts/check.mjs` — единственный набор тестов в проекте. TDD здесь означает: сначала дописать проверки в `check.mjs`, убедиться что они падают, потом реализовать, убедиться что зелено, закоммитить.

**Рабочий каталог всех команд:** `C:\Users\maksi\Desktop\test-ai\plugin` (git-репозиторий).

---

## Решения, зафиксированные в обсуждении

| Блок | Решение |
|---|---|
| A1 | Папка рабочих копий — `repos/` |
| A2 | Имена подпапок задаются в `settings.json` полем `link` (отдельного `dir` нет) |
| A3+A6 | `link` — только локальный путь, относительный от корня рабочего репозитория; git-URL → ошибка конфигурации |
| A4+A5 | Плагин ничего не клонирует; `repoCache`, `.cache/repos/`, `cacheCloneDir` удаляются |
| A8 | `link` и `mainBranch` переезжают из `.env` в `settings.json`; в `.env` остаются только `CONVEYOR_REVIEW_ROUNDS` и `CONVEYOR_FAST` |
| A9 | Поля `remote` нет — setup просто сообщает, что рабочей копии нет |
| B1 | Отдельная нумерация `INTENT-N`, папка на intent: `intents/INTENT-12/intent.md`; задачи в `tasks/` как раньше |
| B3 | В intent нет раздела «Затронутые системы», нет приоритета/сроков/ссылки на трекер |
| B4 | Новая роль `business-analyst` |
| B5 | Цикла ревью на intent нет |
| B7 | Статуса у intent нет |
| B8 | Повторный запуск — «дополнить / перезаписать / отмена» |
| C1 | Одна команда `/conveyor:create-specification` с двумя фазами |
| C2 | Предварительного согласования правок нет |
| C3 | Тип (FE/BE/FE-BE) и номер задаёт аналитик аргументами команды |
| C5 | `specification.md` отдельный для FE и BE; при FE-BE требования разносит агент |
| C6 | Коммит делает скилл после ревью; push/MR — по подтверждению |
| C7 | `feature.md` и его шаблон удаляются |
| D1 | Артефакт `autotest-plan.md`, этап `create-autotest-plan` |
| D2 | План автотестов лежит в папке задачи и строится по её `specification.md` |
| D3 | Предусловие прежнее — `implement-plan` завершён |
| D4 | Сверки после реализации нет |
| D5 | Агент читает `repos/autotests` (соглашения + существующие тесты) read-only |
| D6 | Запускает QA; цикла ревью на этапе нет |
| E1 | Существующий рабочий репозиторий мигрируется скриптом |
| E2 | В `meta.json` добавляется `schemaVersion` |
| E3 | GigaCode-адаптер обновляется синхронно |
| E4 | Версия плагина — 2.0.0 |

**Допущение, не подтверждённое явно (D):** этап `create-autotest-plan` больше не готовит дифф реализации и не открывает кодовую базу; агенту передаются `specification.md` и `plan.md` (раздел «Отклонения и остатки»). Если это неверно — правится только Task 20.

---

## File Structure

**Создаются:**

| Файл | Ответственность |
|---|---|
| `core/scripts/repos-status.mjs` | Состояние рабочих копий: есть / не git / пусто / URL / снаружи. Используют `setup` и `validate-config` |
| `core/scripts/migrate-workspace.mjs` | Миграция рабочего репозитория v1 → v2 (settings.json, meta.json, переименование артефактов) |
| `core/stages/intent.md` | Алгоритм этапа БА |
| `core/stages/create-autotest-plan.md` | Алгоритм этапа плана автотестов (замена `create-requirements-auto-test.md`) |
| `core/templates/intent.md` | Шаблон артефакта intent |
| `core/templates/autotest-plan.md` | Шаблон плана автотестов (замена `requirements-auto-test.md`) |
| `core/prompts/business-analyst.md` | Промпт роли БА |
| `adapters/claude-code/agents/business-analyst.md` | Регистрация субагента |
| `adapters/claude-code/skills/intent/SKILL.md` | Скилл этапа intent |
| `adapters/claude-code/skills/create-autotest-plan/SKILL.md` | Скилл этапа плана автотестов |
| `adapters/gigacode/commands/conveyor/intent.md` | Команда GigaCode |
| `adapters/gigacode/commands/conveyor/create-autotest-plan.md` | Команда GigaCode |

**Изменяются:**

| Файл | Что меняется |
|---|---|
| `core/scripts/lib/config.mjs` | `REPO_DIRS`, `STAGE_NAMES`, резолв `link` в путь, `missingLinks` вместо `missingVars`, удаление `cacheCloneDir`/`repoCache`, `allowedTop` |
| `core/scripts/git-ops.mjs` | `locate` без клонирования, `update` без `kind:cache`, удаление `analysis-head` |
| `core/scripts/validate-config.mjs` | Проверка ссылок и рабочих копий вместо переменных `.env` |
| `core/scripts/validate-artifact.mjs` | Типы: `+intent`, `+autotest-plan`, `−feature`, `−requirements-auto-test`; новые разделы спецификации |
| `core/stages/_common.md` | Таблица рабочих областей, первый шаг этапа, `missingLinks` |
| `core/stages/setup.md` | `repos/`, `intents/`, `.gitignore`, `repos-status` |
| `core/stages/create-specification.md` | Полностью переписывается (две фазы) |
| `core/stages/task-status.md` | Порядок этапов, intent'ы, легаси-артефакты |
| `core/templates/settings.example.json` | Пути вместо `${VAR}`, `mainBranch` рядом, без `repoCache` |
| `core/templates/env.example` | Только два флага |
| `core/templates/specification.md` | Разделы «Границы задачи» и «Внесённые изменения анализа» |
| `core/prompts/system-analyst.md` | Этапы, границы, работа с `.adoc`/`.yml`/`.xml` |
| `core/prompts/qa-autotest-engineer.md` | Этап `create-autotest-plan` вместо требований по диффу |
| `scripts/check.mjs` | Все новые проверки |
| `adapters/claude-code/.claude-plugin/plugin.json` | Версия 2.0.0 |
| `adapters/gigacode/gigacode-extension.json` | Версия 2.0.0 |
| `adapters/gigacode/QWEN.md` | Таблицы команд и областей |
| `README.md`, `INSTALL.md` | Схема, команды, конфигурация, миграция |

**Удаляются:** `core/stages/create-feature.md`, `core/templates/feature.md`, `core/stages/create-requirements-auto-test.md`, `core/templates/requirements-auto-test.md`, `adapters/claude-code/skills/create-feature/`, `adapters/claude-code/skills/create-requirements-auto-test/`, `adapters/gigacode/commands/conveyor/create-feature.md`, `adapters/gigacode/commands/conveyor/create-requirements-auto-test.md`.

---

# Фаза 1 — конфигурация и пути

### Task 1: `REPO_DIRS` и `STAGE_NAMES`

**Files:**
- Modify: `core/scripts/lib/config.mjs:17-29`
- Test: `scripts/check.mjs`

- [ ] **Step 1: Написать падающую проверку**

Сначала расширить статический импорт в шапке `scripts/check.mjs` (строка 13) —
динамический `await import` здесь использовать НЕЛЬЗЯ, это top-level await,
который требует Node 14.8+, а проект заявляет поддержку Node ≥ 14:

```js
import { scopeFilePath, REPO_DIRS, STAGE_NAMES } from '../core/scripts/lib/config.mjs';
```

Затем сразу после блока «1b) settings-шаблон…» добавить:

```js
// 1c) Константы ядра: каталоги репозиториев и имена этапов
{
  const wantDirs = {
    systemsAnalysis: 'repos/system-analysis',
    frontend: 'repos/frontend',
    backend: 'repos/backend',
    autoTest: 'repos/autotests',
  };
  const dirsOk = Object.entries(wantDirs).every(([k, v]) => REPO_DIRS[k] === v);
  if (dirsOk) ok('config: REPO_DIRS — дефолтные каталоги repos/*');
  else bad('config: REPO_DIRS не совпадает с ожидаемым: ' + JSON.stringify(REPO_DIRS));

  const wantStages = [
    'setup',
    'intent',
    'create-specification',
    'create-plan',
    'implement-plan',
    'create-autotest-plan',
    'implement-auto-test',
    'task-status',
  ];
  if (wantStages.every((s) => STAGE_NAMES.includes(s)) && STAGE_NAMES.length === wantStages.length)
    ok('config: STAGE_NAMES — новый список этапов');
  else bad('config: STAGE_NAMES: ' + STAGE_NAMES.join(', '));
}
```

Правило на весь план: в `check.mjs` только статические импорты в шапке.

- [ ] **Step 2: Запустить и убедиться, что падает**

Run: `node scripts/check.mjs`
Expected: FAIL — `config: REPO_DIRS не совпадает…` и `config: STAGE_NAMES: setup, create-feature, …`

- [ ] **Step 3: Реализовать**

В `core/scripts/lib/config.mjs` заменить блок констант (строки 16-29):

```js
// The four repositories, in the order they appear in settings.json.
export const REPO_KEYS = ['systemsAnalysis', 'frontend', 'backend', 'autoTest'];

// Рабочие копии живут ВНУТРИ рабочего репозитория: <workspace>/repos/<dir>.
// Это дефолты для settings.json (repos.<key>.link) и для /setup; команда
// может указать другой путь — он всё равно резолвится от workspaceRoot.
export const REPO_DIRS = {
  systemsAnalysis: 'repos/system-analysis',
  frontend: 'repos/frontend',
  backend: 'repos/backend',
  autoTest: 'repos/autotests',
};

// All pipeline stages (used by scope.mjs validation).
export const STAGE_NAMES = [
  'setup',
  'intent',
  'create-specification',
  'create-plan',
  'implement-plan',
  'create-autotest-plan',
  'implement-auto-test',
  'task-status',
];
```

- [ ] **Step 4: Запустить, убедиться что проверка зелёная**

Run: `node scripts/check.mjs`
Expected: строки `ok config: REPO_DIRS…` и `ok config: STAGE_NAMES…`. Остальные проверки ещё падают (`scope: set create-feature` и др.) — это ожидаемо, чинится в Task 4 и Task 14.

- [ ] **Step 5: Коммит**

```bash
git add core/scripts/lib/config.mjs scripts/check.mjs && git commit -m "feat(config): REPO_DIRS и новый список этапов"
```

---

### Task 2: `link` резолвится в путь, `missingLinks` вместо `missingVars`

**Files:**
- Modify: `core/scripts/lib/config.mjs:103-199` (тело `readConfig`)
- Modify: `core/scripts/lib/config.mjs:284-297` (`cacheCloneDir`, `repoRootFor`)
- Test: `scripts/check.mjs`

- [ ] **Step 1: Написать падающую проверку**

В `scripts/check.mjs` в блоке «3) Скрипты во временном workspace» заменить создание `settings.json`/`.env` и проверки `resolve-config` на новые. Найти строки от `fs.writeFileSync(path.join(tmp, 'settings.json'), …)` до строки с `resolve-config: mainBranch не main по умолчанию` и заменить целиком на:

```js
  // Рабочие копии лежат внутри workspace: repos/<dir>
  const repoSA = path.join(tmp, 'repos', 'system-analysis');
  const repoBE = path.join(tmp, 'repos', 'backend');
  fs.mkdirSync(path.join(repoSA, '.git'), { recursive: true });
  fs.mkdirSync(path.join(repoBE, '.git'), { recursive: true });
  fs.writeFileSync(
    path.join(tmp, 'settings.json'),
    JSON.stringify(
      {
        taskPrefix: 'TASK',
        repos: {
          systemsAnalysis: { link: 'repos/system-analysis', mainBranch: 'main' },
          frontend: { link: '', mainBranch: 'main' },
          backend: { link: 'repos/backend', mainBranch: 'master' },
          autoTest: { link: '', mainBranch: 'main' },
        },
        reviewRounds: '${CONVEYOR_REVIEW_ROUNDS}',
        fast: '${CONVEYOR_FAST}',
        language: 'ru',
      },
      null,
      2,
    ),
  );
  fs.writeFileSync(path.join(tmp, '.env'), 'CONVEYOR_REVIEW_ROUNDS=\nCONVEYOR_FAST=\n');

  const rc = JSON.parse(runScript('core/scripts/resolve-config.mjs', [tmp]));
  if (rc.found && rc.links.systemsAnalysis.path === path.resolve(repoSA))
    ok('resolve-config: link резолвится в абсолютный путь от workspaceRoot');
  else bad('resolve-config: путь не разрезолвился: ' + JSON.stringify(rc.links && rc.links.systemsAnalysis));
  if (rc.missingLinks && rc.missingLinks.includes('frontend') && rc.missingLinks.includes('autoTest'))
    ok('resolve-config: пустые ссылки попадают в missingLinks');
  else bad('resolve-config: missingLinks: ' + JSON.stringify(rc.missingLinks));
  if (rc.missingLinks && !rc.missingLinks.includes('systemsAnalysis'))
    ok('resolve-config: заполненная ссылка не в missingLinks');
  else bad('resolve-config: заполненная ссылка ошибочно в missingLinks');
  if (rc.links.backend.mainBranch === 'master' && rc.links.autoTest.mainBranch === 'main')
    ok('resolve-config: mainBranch читается из settings.json');
  else bad('resolve-config: mainBranch: ' + JSON.stringify([rc.links.backend.mainBranch, rc.links.autoTest.mainBranch]));
  if (rc.links.systemsAnalysis.inside === true) ok('resolve-config: inside=true для пути внутри workspace');
  else bad('resolve-config: inside не выставлен');
  if (rc.repoCacheEnabled === undefined) ok('resolve-config: repoCacheEnabled удалён');
  else bad('resolve-config: repoCacheEnabled ещё возвращается');
  if (rc.config && rc.config.reviewRounds === 2) ok('resolve-config: reviewRounds по умолчанию = 2');
  else bad('resolve-config: reviewRounds не 2: ' + (rc.config && rc.config.reviewRounds));
```

- [ ] **Step 2: Запустить и убедиться, что падает**

Run: `node scripts/check.mjs`
Expected: FAIL — `resolve-config: путь не разрезолвился`, `missingLinks: undefined`, `repoCacheEnabled ещё возвращается`

- [ ] **Step 3: Реализовать**

В `core/scripts/lib/config.mjs` заменить всё от строки `const config = deep(settings);` до `return { found: true, workspaceRoot, config, repoCacheEnabled, fastMode, links, missingVars };` на:

```js
  const config = deep(settings);
  // fast mode (CONVEYOR_FAST): default false; forces reviewRounds = 0.
  const fastMode = normalizeBool(config.fast);
  config.fast = fastMode;
  // reviewRounds comes from env (${VAR}); empty/invalid -> default 2.
  config.reviewRounds = fastMode ? 0 : normalizeReviewRounds(config.reviewRounds);

  // Ссылка на репозиторий — путь к рабочей копии ОТНОСИТЕЛЬНО workspaceRoot
  // (по умолчанию repos/<dir>). Абсолютный путь тоже допустим, но выводит за
  // пределы проекта: помечаем inside=false — GigaCode такое не разрешит.
  const links = {};
  const missingLinks = [];
  const urlLinks = [];
  const rawRepos = settings.repos || {};
  for (const key of REPO_KEYS) {
    const value = (((config.repos && config.repos[key] && config.repos[key].link) || '') + '').trim();
    const mainBranch =
      (((config.repos && config.repos[key] && config.repos[key].mainBranch) || '') + '').trim() || 'main';
    if (config.repos && config.repos[key]) config.repos[key].mainBranch = mainBranch;

    const url = isGitUrl(value);
    const abs = value && !url ? path.resolve(workspaceRoot, value) : null;
    if (!value) missingLinks.push(key);
    if (url) urlLinks.push(key);

    links[key] = {
      value,
      isGitUrl: url,
      resolved: value !== '' && !url,
      path: abs,
      inside: abs ? isInside(abs, workspaceRoot) : false,
      mainBranch,
    };
  }

  return {
    found: true,
    workspaceRoot,
    config,
    fastMode,
    links,
    missingLinks,
    urlLinks,
  };
```

Из шапки `readConfig` (комментарий строк 93-102) убрать упоминания `repoCacheEnabled` и `missingVars`, вписав:

```js
// Read + resolve. Returns a rich object; never throws for the common cases.
//   { found:false }                              — no settings.json
//   { found:true, error:'...' }                  — settings.json unparseable
//   { found:true, workspaceRoot, config,         — success
//     fastMode, links, missingLinks, urlLinks }
//
// links[key] = { value, isGitUrl, resolved, path, inside, mainBranch }
// missingLinks = repo keys with an empty link; urlLinks = keys where the link
// is a git URL (a configuration error: the plugin never clones).
```

Удалить целиком `cacheCloneDir` (строки 284-288) и заменить `repoRootFor`:

```js
// Working-copy root for a repo key: absolute path resolved from the workspace
// root. null when the link is empty or a (rejected) git URL.
export function repoRootFor(cfg, key) {
  const l = cfg.links && cfg.links[key];
  if (!l || !l.resolved) return null;
  return l.path;
}
```

Также убрать `.cache` из `allowedRoots` и снять фильтр по `isGitUrl`:

```js
export function allowedRoots(cfg) {
  const roots = [cfg.workspaceRoot, os.tmpdir()];
  for (const key of REPO_KEYS) {
    const root = repoRootFor(cfg, key);
    if (root) roots.push(root);
  }
  return roots.map((r) => path.resolve(r));
}
```

- [ ] **Step 4: Запустить, убедиться что новые проверки зелёные**

Run: `node scripts/check.mjs`
Expected: шесть строк `ok resolve-config: …`. Падения в блоках guard/scope остаются — Task 3 и Task 4.

- [ ] **Step 5: Коммит**

```bash
git add core/scripts/lib/config.mjs scripts/check.mjs && git commit -m "feat(config): link -> путь от workspaceRoot, missingLinks вместо missingVars"
```

---

### Task 3: `checkWrite` — разрешить `repos/` и `intents/`

**Files:**
- Modify: `core/scripts/lib/config.mjs:390-484` (`checkWrite`)
- Test: `scripts/check.mjs`

- [ ] **Step 1: Написать падающую проверку**

В `scripts/check.mjs`, в блоке рабочей области, после проверки `scope: артефакты задачи всегда разрешены` добавить:

```js
  // intents/ — артефакты БА, писать можно при активном этапе
  if (writeTo(path.join(tmp, 'intents/INTENT-1/intent.md')) === '')
    ok('scope: intents/ разрешён при активном этапе');
  else bad('scope: intents/ заблокирован');
  // repos/<незаконфигуренный> при активном этапе — deny (это чужая рабочая копия)
  const denyUnknownRepo = writeTo(path.join(tmp, 'repos/unknown/x.js'));
  if (denyUnknownRepo && JSON.parse(denyUnknownRepo).hookSpecificOutput.permissionDecision === 'deny')
    ok('scope: запись в незаконфигуренный repos/* заблокирована');
  else bad('scope: незаконфигуренный repos/* прошёл');
```

- [ ] **Step 2: Запустить и убедиться, что падает**

Run: `node scripts/check.mjs`
Expected: FAIL — `scope: intents/ заблокирован` (текущий `allowedTop` его не содержит)

- [ ] **Step 3: Реализовать**

В `checkWrite` удалить блок про `.cache/repos` (строки 442-449) целиком и заменить блок `allowedTop`:

```js
    // При активном этапе workspace — только артефакты: tasks/, intents/,
    // repos/ (рабочие копии проверены выше как kind:'repo') и файлы
    // конфигурации. Пробные/временные файлы в корне запрещены — временное
    // пишите в системный temp.
    if (scopedKeys || corrupt) {
      const rel = path.relative(wsRoot, target);
      const top = rel.split(path.sep)[0];
      const allowedTop = ['tasks', 'intents', 'settings.json', '.env', '.env.example', '.gitignore'];
      if (rel !== '' && !allowedTop.includes(top)) {
        return {
          allowed: false,
          reason:
            `при активном этапе в рабочем репозитории запись разрешена только в tasks/ и intents/ ` +
            `(запрошено: ${rel}); рабочие копии — только те, что в области этапа; ` +
            `временные файлы — в системный temp`,
        };
      }
```

`repos` намеренно НЕ входит в `allowedTop`: путь внутри настроенной рабочей копии до этого места не доходит (он матчится как `kind:'repo'` по самому глубокому корню), а всё остальное в `repos/` — чужой или неизвестный клон, писать туда нельзя.

- [ ] **Step 4: Запустить, убедиться что зелено**

Run: `node scripts/check.mjs`
Expected: `ok scope: intents/ разрешён при активном этапе`, `ok scope: запись в незаконфигуренный repos/* заблокирована`

- [ ] **Step 5: Коммит**

```bash
git add core/scripts/lib/config.mjs scripts/check.mjs && git commit -m "feat(guard): intents/ разрешён, чужие клоны в repos/ заблокированы"
```

---

### Task 4: `stageWriteRepoKeys`, `requiredRepoKeys` и `--write none`

**Files:**
- Modify: `core/scripts/lib/config.mjs:216-235` (`requiredRepoKeys`), `:266-280` (`stageWriteRepoKeys`)
- Modify: `core/scripts/scope.mjs:80-87`
- Test: `scripts/check.mjs`

- [ ] **Step 1: Написать падающую проверку**

В `scripts/check.mjs` заменить старые scope-проверки: строку с `['set', '--stage', 'create-feature', '--type', 'BE', '--task', 'TASK-1']` и следующий за ней `if` — на:

```js
  const setOut = JSON.parse(
    runScript('core/scripts/scope.mjs', ['set', '--stage', 'create-specification', '--type', 'BE', '--task', 'TASK-1'], '', tmp),
  );
  if (setOut.ok && setOut.scope.writeRepos.join(',') === 'systemsAnalysis')
    ok('scope: create-specification (фаза A) → writeRepos=[systemsAnalysis]');
  else bad('scope: set вернул неожиданное: ' + JSON.stringify(setOut));
```

И после проверки `scope: create-plan — запись в репо запрещена` добавить:

```js
  // --write none: фаза B спецификации снимает право записи в анализ
  const noneOut = JSON.parse(
    runScript('core/scripts/scope.mjs', ['set', '--stage', 'create-specification', '--type', 'BE', '--write', 'none'], '', tmp),
  );
  if (noneOut.ok && noneOut.scope.writeRepos.length === 0) ok('scope: --write none → writeRepos=[]');
  else bad('scope: --write none не сработал: ' + JSON.stringify(noneOut));
  const denySAphaseB = writeTo(path.join(repoSA, 'doc.adoc'));
  if (denySAphaseB && JSON.parse(denySAphaseB).hookSpecificOutput.permissionDecision === 'deny')
    ok('scope: в фазе B запись в анализ заблокирована');
  else bad('scope: фаза B не заблокировала запись в анализ');
  // план автотестов читает автотесты, но не пишет никуда
  const apOut = JSON.parse(
    runScript('core/scripts/scope.mjs', ['set', '--stage', 'create-autotest-plan', '--type', 'FE'], '', tmp),
  );
  if (apOut.ok && apOut.scope.writeRepos.length === 0) ok('scope: create-autotest-plan → writeRepos=[]');
  else bad('scope: create-autotest-plan: ' + JSON.stringify(apOut));
```

Ниже по файлу заменить два оставшихся вхождения `['set', '--stage', 'create-feature', '--type', 'FE']` на `['set', '--stage', 'create-specification', '--type', 'FE']`.

- [ ] **Step 2: Запустить и убедиться, что падает**

Run: `node scripts/check.mjs`
Expected: FAIL — `scope: set вернул неожиданное: {"ok":false,"error":"неизвестный этап…"}` уже не будет (Task 1 добавил имя), но `writeRepos` для `create-specification` придёт пустым, и `--write none` вернёт дефолт.

- [ ] **Step 3: Реализовать**

В `core/scripts/lib/config.mjs`:

```js
// Which repos a stage needs (spec 4.4). taskType is 'FE' | 'BE' | undefined.
export function requiredRepoKeys(stage, taskType) {
  const code = taskType === 'BE' ? 'backend' : 'frontend';
  switch (stage) {
    case 'setup':
    case 'task-status':
      return [];
    case 'intent':
    case 'create-specification':
      return ['systemsAnalysis'];
    case 'create-plan':
    case 'implement-plan':
      return [code];
    case 'create-autotest-plan':
    case 'implement-auto-test':
      return ['autoTest'];
    default:
      return [];
  }
}
```

```js
// Which repos a stage may WRITE (narrower than requiredRepoKeys: read-only
// stages get []). taskType is 'FE' | 'BE' | 'FE-BE' | undefined.
// create-specification пишет в анализ только в фазе A; фаза B вызывает
// scope.mjs set --write none.
export function stageWriteRepoKeys(stage, taskType) {
  const code = taskType === 'BE' ? 'backend' : 'frontend';
  switch (stage) {
    case 'create-specification':
      return ['systemsAnalysis'];
    case 'implement-plan':
      return [code];
    case 'implement-auto-test':
      return ['autoTest'];
    // setup, task-status, intent, create-plan, create-autotest-plan:
    // artifacts only — no repo writes.
    default:
      return [];
  }
}
```

В `core/scripts/scope.mjs` заменить блок разбора `--write` (строки 80-87):

```js
  let writeRepos;
  if (args.write === 'none') {
    writeRepos = []; // явный запрет записи в репозитории (фаза B спецификации)
  } else if (typeof args.write === 'string') {
    writeRepos = args.write.split(',').map((s) => s.trim()).filter(Boolean);
    const bad = writeRepos.filter((k) => !REPO_KEYS.includes(k));
    if (bad.length) done({ ok: false, error: `неизвестные репозитории: ${bad.join(', ')}` });
  } else {
    writeRepos = stageWriteRepoKeys(args.stage, taskType);
  }
```

И в usage-шапке `scope.mjs` дописать строку:

```js
//                      [--write key1,key2|none]   # override; иначе по этапу
```

- [ ] **Step 4: Запустить, убедиться что зелено**

Run: `node scripts/check.mjs`
Expected: все `ok scope: …`

- [ ] **Step 5: Коммит**

```bash
git add core/scripts/lib/config.mjs core/scripts/scope.mjs scripts/check.mjs && git commit -m "feat(scope): области новых этапов и --write none"
```

---

### Task 5: `git-ops locate` без клонирования

**Files:**
- Modify: `core/scripts/git-ops.mjs:19` (импорт), `:86-118` (`cmdLocate`), `:131-184` (`cmdUpdate`), `:239-278` (удалить `cmdAnalysisHead`), `:280-310` (dispatch), `:1-14` (шапка)
- Test: `scripts/check.mjs`

- [ ] **Step 1: Написать падающую проверку**

В `scripts/check.mjs` после блока resolve-config добавить:

```js
  // git-ops locate: путь от workspace, без клонирования
  const locOk = JSON.parse(
    runScript('core/scripts/git-ops.mjs', ['locate', '--link', 'repos/system-analysis', '--workspace', tmp, '--name', 'systemsAnalysis']),
  );
  if (locOk.ok && locOk.path === path.resolve(repoSA)) ok('git-ops locate: относительный путь резолвится от workspace');
  else bad('git-ops locate: ' + JSON.stringify(locOk));

  let locUrl = '';
  try {
    locUrl = runScript('core/scripts/git-ops.mjs', ['locate', '--link', 'git@host:group/repo.git', '--workspace', tmp, '--name', 'frontend']);
  } catch (e) {
    locUrl = String(e.stdout || '');
  }
  if (/git-URL/.test(locUrl)) ok('git-ops locate: git-URL отклоняется с подсказкой');
  else bad('git-ops locate: URL не отклонён: ' + locUrl);

  let locMissing = '';
  try {
    locMissing = runScript('core/scripts/git-ops.mjs', ['locate', '--link', 'repos/nope', '--workspace', tmp, '--name', 'autoTest']);
  } catch (e) {
    locMissing = String(e.stdout || '');
  }
  if (/не найдена/.test(locMissing)) ok('git-ops locate: отсутствующая рабочая копия — понятная ошибка');
  else bad('git-ops locate: нет ошибки про отсутствующую копию: ' + locMissing);
```

- [ ] **Step 2: Запустить и убедиться, что падает**

Run: `node scripts/check.mjs`
Expected: FAIL — `git-ops locate` вернёт `локальный путь не найден: repos/system-analysis` (резолв идёт от cwd, а не от workspace)

- [ ] **Step 3: Реализовать**

В `core/scripts/git-ops.mjs` заменить импорт строки 19:

```js
import { isGitUrl } from './lib/config.mjs';
```

Заменить `cmdLocate` целиком:

```js
function cmdLocate(a) {
  const link = a.link;
  const workspace = a.workspace;
  const name = a.name || 'repo';
  if (!link) return fail('locate: --link required');
  if (!workspace) return fail('locate: --workspace required');

  if (isGitUrl(link)) {
    return fail(
      `ссылка ${name} задана git-URL (${link}). Плагин не клонирует репозитории: ` +
        `укажите в settings.json путь к рабочей копии (например repos/${name}) ` +
        'и склонируйте репозиторий туда самостоятельно.',
    );
  }

  const dest = path.resolve(workspace, link);
  if (!fs.existsSync(dest)) {
    return fail(
      `рабочая копия ${name} не найдена: ${dest}. Склонируйте репозиторий в этот каталог.`,
    );
  }
  if (!fs.existsSync(path.join(dest, '.git'))) return fail(`не git-репозиторий: ${dest}`);
  return done({ ok: true, path: dest, kind: 'local' });
}
```

В `cmdUpdate` удалить ветку кэша — заменить блок `if (kind === 'cache') { … } else { … }` на его `else`-часть без обёртки:

```js
  // Рабочая копия принадлежит разработчику: НИКОГДА не переключаем его ветку.
  const cur = currentBranch(repoPath);
  if (cur === main) {
    if (clean) {
      const pull = tryGit(repoPath, ['pull', '--ff-only']);
      if (!pull.ok) warnings.push(`pull: ${pull.out}`);
    } else {
      warnings.push('working tree грязный: обновление пропущено, база — origin/' + main);
    }
  } else {
    // Update main without checking it out.
    const upd = tryGit(repoPath, ['fetch', 'origin', `${main}:${main}`]);
    if (!upd.ok) warnings.push(`обновление ${main} без переключения не удалось: ${upd.out}`);
  }
```

И убрать из `cmdUpdate` строку `const kind = a.kind || 'local';`.

Удалить функцию `cmdAnalysisHead` целиком (строки 239-278) и её ветку в dispatch:

```js
    case 'analysis-head':
      cmdAnalysisHead(args);
      break;
```

Обновить шапку файла (строки 8-14):

```js
// Subcommands:
//   locate       --link <path> --workspace <root> --name <key>
//   clean-check  --path <p>
//   update       --path <p> --main <branch> --mode read|write
//   log          --path <p> [--main <branch>] [-n <count>]
//   branch       --path <p> --branch <name> --from <mainBranch>
//   diff         --path <p> --base <ref> --head <ref>
```

- [ ] **Step 4: Запустить, убедиться что зелено**

Run: `node scripts/check.mjs`
Expected: три `ok git-ops locate: …`

- [ ] **Step 5: Коммит**

```bash
git add core/scripts/git-ops.mjs scripts/check.mjs && git commit -m "feat(git-ops): locate резолвит путь от workspace, клонирование и analysis-head удалены"
```

---

### Task 6: Скрипт `repos-status.mjs`

**Files:**
- Create: `core/scripts/repos-status.mjs`
- Test: `scripts/check.mjs`

- [ ] **Step 1: Написать падающую проверку**

В `scripts/check.mjs` после проверок `git-ops locate` добавить:

```js
  // repos-status: состояние рабочих копий
  const rs = JSON.parse(runScript('core/scripts/repos-status.mjs', [tmp]));
  const byKey = Object.fromEntries(rs.repos.map((r) => [r.key, r]));
  if (byKey.systemsAnalysis.state === 'ok') ok('repos-status: существующая копия → ok');
  else bad('repos-status: systemsAnalysis: ' + JSON.stringify(byKey.systemsAnalysis));
  if (byKey.frontend.state === 'link-empty') ok('repos-status: пустая ссылка → link-empty');
  else bad('repos-status: frontend: ' + JSON.stringify(byKey.frontend));
  if (rs.summary.ok === 2 && rs.summary.problems === 2) ok('repos-status: сводка посчитана');
  else bad('repos-status: сводка: ' + JSON.stringify(rs.summary));
```

- [ ] **Step 2: Запустить и убедиться, что падает**

Run: `node scripts/check.mjs`
Expected: FAIL — `Cannot find module …repos-status.mjs`

- [ ] **Step 3: Реализовать**

Создать `core/scripts/repos-status.mjs`:

```js
#!/usr/bin/env node
// repos-status.mjs — состояние рабочих копий репозиториев конвейера.
// Используют /setup (таблица «что настроено») и validate-config (SessionStart).
// Плагин НЕ клонирует: задача скрипта — внятно сказать, чего не хватает.
//
// Usage: node repos-status.mjs [workspaceRoot]
// Output (stdout, JSON):
//   { ok, workspaceRoot, repos: [{ key, link, path, state, branch, clean, hint }],
//     summary: { ok, problems } }
// state: ok | missing | not-a-repo | link-empty | link-is-url | outside

import fs from 'node:fs';
import path from 'node:path';
import { execFileSync } from 'node:child_process';
import { readConfig, REPO_KEYS, REPO_DIRS } from './lib/config.mjs';

function git(cwd, args) {
  try {
    return execFileSync('git', args, { cwd, encoding: 'utf8', stdio: ['ignore', 'pipe', 'pipe'] }).trim();
  } catch {
    return null;
  }
}

const cfg = readConfig(process.argv[2] || process.cwd());
if (!cfg.found) {
  process.stdout.write(JSON.stringify({ ok: false, error: 'не найден рабочий репозиторий conveyor (settings.json)' }) + '\n');
  process.exit(1);
}

const repos = [];
for (const key of REPO_KEYS) {
  const l = cfg.links[key];
  const entry = { key, link: l.value, path: l.path, state: 'ok', branch: null, clean: null, hint: null };

  if (!l.value) {
    entry.state = 'link-empty';
    entry.hint = `заполните repos.${key}.link в settings.json (обычно ${REPO_DIRS[key]})`;
  } else if (l.isGitUrl) {
    entry.state = 'link-is-url';
    entry.hint = `в settings.json нужен путь, а не URL: укажите ${REPO_DIRS[key]} и склонируйте репозиторий туда`;
  } else if (!fs.existsSync(l.path)) {
    entry.state = 'missing';
    entry.hint = `склонируйте репозиторий в ${l.value}`;
  } else if (!fs.existsSync(path.join(l.path, '.git'))) {
    entry.state = 'not-a-repo';
    entry.hint = `каталог ${l.value} существует, но это не git-репозиторий`;
  } else {
    entry.branch = git(l.path, ['rev-parse', '--abbrev-ref', 'HEAD']);
    const status = git(l.path, ['status', '--porcelain']);
    entry.clean = status === '';
    if (!l.inside) {
      entry.state = 'outside';
      entry.hint = 'путь выходит за пределы рабочего репозитория — GigaCode такую конфигурацию не разрешит';
    }
  }
  repos.push(entry);
}

const okCount = repos.filter((r) => r.state === 'ok').length;
process.stdout.write(
  JSON.stringify(
    { ok: true, workspaceRoot: cfg.workspaceRoot, repos, summary: { ok: okCount, problems: repos.length - okCount } },
    null,
    2,
  ) + '\n',
);
```

- [ ] **Step 4: Запустить, убедиться что зелено**

Run: `node scripts/check.mjs`
Expected: три `ok repos-status: …`

- [ ] **Step 5: Коммит**

```bash
git add core/scripts/repos-status.mjs scripts/check.mjs && git commit -m "feat(scripts): repos-status — состояние рабочих копий"
```

---

### Task 7: Шаблоны конфигурации

**Files:**
- Modify: `core/templates/settings.example.json`
- Modify: `core/templates/env.example`
- Modify: `scripts/check.mjs:52-59`

- [ ] **Step 1: Обновить проверку ключей шаблона**

В `scripts/check.mjs` заменить блок «1b»:

```js
// 1b) settings-шаблон содержит ожидаемые ключи и НЕ содержит удалённых
{
  const st = JSON.parse(fs.readFileSync(path.join(root, 'core/templates/settings.example.json'), 'utf8'));
  const need = ['taskPrefix', 'repos', 'reviewRounds', 'fast', 'language'];
  const miss = need.filter((k) => !(k in st));
  if (!miss.length) ok('settings.example.json: все ключи на месте (' + need.join(', ') + ')');
  else bad('settings.example.json: нет ключей: ' + miss.join(', '));
  if (!('repoCache' in st)) ok('settings.example.json: repoCache удалён');
  else bad('settings.example.json: repoCache ещё есть');
  const linksOk = Object.values(st.repos).every((r) => typeof r.link === 'string' && r.link.startsWith('repos/'));
  if (linksOk) ok('settings.example.json: ссылки — пути repos/*');
  else bad('settings.example.json: ссылки не пути: ' + JSON.stringify(st.repos));
}
```

- [ ] **Step 2: Запустить и убедиться, что падает**

Run: `node scripts/check.mjs`
Expected: FAIL — `settings.example.json: repoCache ещё есть`, `ссылки не пути`

- [ ] **Step 3: Реализовать**

`core/templates/settings.example.json`:

```json
{
  "taskPrefix": "TASK",
  "repos": {
    "systemsAnalysis": { "link": "repos/system-analysis", "mainBranch": "main" },
    "frontend":        { "link": "repos/frontend",        "mainBranch": "main" },
    "backend":         { "link": "repos/backend",         "mainBranch": "main" },
    "autoTest":        { "link": "repos/autotests",       "mainBranch": "main" }
  },
  "reviewRounds": "${CONVEYOR_REVIEW_ROUNDS}",
  "fast": "${CONVEYOR_FAST}",
  "language": "ru"
}
```

`core/templates/env.example`:

```
# Личные настройки разработчика. Ссылки на репозитории и основные ветки
# живут в settings.json (он одинаковый у всей команды и коммитится).
# Этот файл в .gitignore и не обязателен: без него действуют умолчания.

# Число раундов цикла ревью (пусто = 2 по умолчанию; 0 — ревью выключено)
CONVEYOR_REVIEW_ROUNDS=

# Быстрый режим для медленных/слабых моделей (true — включить; пусто или
# false — выключен, значение по умолчанию). Включает: ревью отключено,
# валидация артефактов скриптом, короче git-контекст и диффы.
CONVEYOR_FAST=
```

- [ ] **Step 4: Запустить, убедиться что зелено**

Run: `node scripts/check.mjs`
Expected: `ok settings.example.json: repoCache удалён`, `ok settings.example.json: ссылки — пути repos/*`

- [ ] **Step 5: Коммит**

```bash
git add core/templates/settings.example.json core/templates/env.example scripts/check.mjs && git commit -m "feat(templates): settings.json с путями repos/*, .env только под личные флаги"
```

---

### Task 8: `validate-config.mjs` — проверки ссылок вместо переменных

**Files:**
- Modify: `core/scripts/validate-config.mjs:17-33`, `:93-124`, `:172-189`

- [ ] **Step 1: Написать падающую проверку**

В `scripts/check.mjs` перед `} finally {` добавить:

```js
  // validate-config: предупреждает о незаполненных ссылках
  runScript('core/scripts/scope.mjs', ['clear'], '', tmp);
  const vcOut = runScript('core/scripts/validate-config.mjs', [], JSON.stringify({ cwd: tmp })).trim();
  const vcCtx = vcOut ? JSON.parse(vcOut).hookSpecificOutput.additionalContext : '';
  if (/frontend/.test(vcCtx) && /autoTest/.test(vcCtx)) ok('validate-config: сообщает о незаполненных ссылках');
  else bad('validate-config: нет предупреждения о ссылках: ' + vcCtx);
  if (!/\.env/.test(vcCtx) || /settings\.json/.test(vcCtx)) ok('validate-config: отсылает к settings.json');
  else bad('validate-config: всё ещё отсылает к .env: ' + vcCtx);
```

- [ ] **Step 2: Запустить и убедиться, что падает**

Run: `node scripts/check.mjs`
Expected: FAIL — контекст ссылается на `.env` и `missingVars`

- [ ] **Step 3: Реализовать**

Заменить список этапов (строки 26-33):

```js
const STAGES_NEEDING_REPO = [
  'intent',
  'create-specification',
  'create-plan',
  'implement-plan',
  'create-autotest-plan',
  'implement-auto-test',
];
```

Заменить импорт `REPO_KEYS, LEGACY_SCOPE_FILE` — `REPO_KEYS` больше не нужен, `LEGACY_SCOPE_FILE` остаётся:

```js
import {
  readConfig,
  readScopeState,
  requiredRepoKeys,
  scopeFilePath,
  LEGACY_SCOPE_FILE,
} from './lib/config.mjs';
```

Заменить блок проверок (строки 95-124):

```js
  if (cfg.error) {
    lines.push(`⚠ conveyor: ${cfg.error}`);
  } else {
    const problems = cfg.missingLinks.concat(cfg.urlLinks);
    if (problems.length) {
      const blocked = new Set();
      for (const stage of STAGES_NEEDING_REPO) {
        const keys = requiredRepoKeys(stage, 'FE').concat(requiredRepoKeys(stage, 'BE'));
        for (const key of new Set(keys)) {
          if (problems.includes(key)) blocked.add(stage);
        }
      }
      if (cfg.missingLinks.length) {
        lines.push(
          `⚠ conveyor: в settings.json не заполнены ссылки: ${cfg.missingLinks.join(', ')}.`,
        );
      }
      if (cfg.urlLinks.length) {
        lines.push(
          `⚠ conveyor: ссылки заданы git-URL (${cfg.urlLinks.join(', ')}), а нужен путь к рабочей копии ` +
            '(например repos/frontend) — плагин не клонирует репозитории.',
        );
      }
      if (blocked.size) lines.push(`Заблокированы этапы: ${[...blocked].join(', ')}.`);
    }
  }
```

Заменить блок легаси-уборки `.cache` (строки 172-189) — оставить только удаление старого scope-файла и добавить подсказку про миграцию:

```js
  // Легаси: рабочий репозиторий из conveyor 1.x. Старый scope-файл убираем,
  // про устаревший settings.json сообщаем — миграция отдельным скриптом.
  try {
    fs.unlinkSync(path.join(cfg.workspaceRoot, LEGACY_SCOPE_FILE));
  } catch {
    /* отсутствует */
  }
  if (cfg.urlLinks.length || fs.existsSync(path.join(cfg.workspaceRoot, '.cache', 'repos'))) {
    lines.push(
      'conveyor: похоже, это рабочий репозиторий версии 1.x. Миграция: ' +
        'node <plugin>/core/scripts/migrate-workspace.mjs --apply',
    );
  }
```

- [ ] **Step 4: Запустить, убедиться что зелено**

Run: `node scripts/check.mjs`
Expected: два `ok validate-config: …`

- [ ] **Step 5: Коммит**

```bash
git add core/scripts/validate-config.mjs scripts/check.mjs && git commit -m "feat(validate-config): проверка ссылок settings.json вместо переменных .env"
```

---

### Task 9: Этап `/setup` под новую структуру

**Files:**
- Modify: `core/stages/setup.md`

- [ ] **Step 1: Переписать стейдж**

Полностью заменить содержимое `core/stages/setup.md`:

```markdown
# Этап /setup — инициализация рабочего репозитория

**Агент:** не требуется (выполняет основная сессия).
**Аргументы:** нет.

Команда выполняет две роли: первичная инициализация (тимлид, один раз) и
диагностика окружения (каждый разработчик после клонирования рабочего
репозитория). Второй сценарий — основной, поэтому ничего не перезаписывай
молча.

## Алгоритм
1. Если `settings.json` уже есть в текущем каталоге — НЕ трогай его: скажи,
   что репозиторий уже инициализирован, и переходи к шагу 5 (диагностика).
   Существование файлов проверяй инструментами платформы или
   `node -e "...fs.existsSync..."` — не shell-командами (`dir`, `> nul`).
2. Спроси у пользователя `taskPrefix` (по умолчанию `TASK`).
3. Создай структуру:
   - каталоги `tasks/FE/`, `tasks/BE/`, `intents/`, `repos/`;
   - `settings.json` и `.env.example` — СКОПИРУЙ ФАЙЛЫ шаблонов МЕХАНИЧЕСКИ
     (НЕ набирай содержимое по памяти — потеряешь новые ключи):
     `node -e "const f=require('fs');f.copyFileSync('<CONVEYOR_ROOT>/core/templates/settings.example.json','settings.json');f.copyFileSync('<CONVEYOR_ROOT>/core/templates/env.example','.env.example')"`
     Затем, если пользователь выбрал не-дефолтный taskPrefix, — точечная
     правка ТОЛЬКО этого значения в скопированном settings.json.
   Самопроверка: сверь ключи созданного settings.json с шаблоном —
   `node -e "const a=Object.keys(require('<CONVEYOR_ROOT>/core/templates/settings.example.json')),b=Object.keys(require('./settings.json'));const m=a.filter(k=>!b.includes(k));if(m.length){console.log('missing:',m.join(','));process.exit(1)}"`
   — при missing дополни файл недостающими ключами из шаблона.
4. Запиши `.gitignore` ДО того, как в `repos/` что-то появится (иначе первый
   же `git status` затянет чужие рабочие деревья). Нужны строки:
   `repos/` и `.env`. Файла нет — создай; есть — допиши недостающие строки,
   ничего не удаляя.
5. Диагностика рабочих копий:
   `node "${CONVEYOR_ROOT}/core/scripts/repos-status.mjs"`
   Выведи таблицу по каждому репозиторию: ключ, путь, состояние, ветка,
   чистота рабочего дерева, подсказка из `hint`. Ни одно неуспешное
   состояние не прерывает остальные проверки.
6. Если рядом есть признаки версии 1.x (`.cache/repos/`, ссылки git-URL в
   settings.json, `feature.md` в папках задач) — предложи миграцию:
   `node "${CONVEYOR_ROOT}/core/scripts/migrate-workspace.mjs"` (сначала без
   `--apply` — покажет план), затем с `--apply`.

## Ошибки
Плагин НИЧЕГО не клонирует. Для состояний `missing` / `link-is-url` покажи
путь, куда нужна рабочая копия, и скажи, что репозиторий клонирует сам
разработчик. Для `outside` предупреди, что путь выходит за пределы рабочего
репозитория и на GigaCode такая конфигурация не поедет.

## Вывод
Таблица `repos-status`, список того, что осталось сделать руками
(склонировать / поправить ссылку), и следующий шаг — `/conveyor:intent`.

## DoD
Структура создана (`tasks/FE`, `tasks/BE`, `intents/`, `repos/`,
`settings.json`, `.env.example`, `.gitignore`); диагностика выполнена;
пользователю показано, что настроено и что осталось.
```

- [ ] **Step 2: Проверить, что check.mjs всё ещё зелёный**

Run: `node scripts/check.mjs`
Expected: без изменений (стейдж-файлы проверяются только на существование)

- [ ] **Step 3: Коммит**

```bash
git add core/stages/setup.md && git commit -m "feat(setup): repos/, intents/, .gitignore и диагностика вместо проверки ссылок"
```

---

# Фаза 2 — миграция существующего репозитория

### Task 10: Скрипт `migrate-workspace.mjs`

**Files:**
- Create: `core/scripts/migrate-workspace.mjs`
- Test: `scripts/check.mjs`

Миграция делает четыре вещи: переписывает `settings.json` (ссылки `${VAR}` → пути `repos/*`, `mainBranch` из `.env`, убирает `repoCache`), добавляет `schemaVersion: 2` и переименовывает этапы в каждом `meta.json`, переименовывает `requirements-auto-test.md` → `autotest-plan.md`, дописывает `.gitignore`. По умолчанию — сухой прогон.

- [ ] **Step 1: Написать падающую проверку**

В `scripts/check.mjs` перед `} finally {` добавить:

```js
  // ---- миграция рабочего репозитория 1.x -> 2.0 ----
  {
    const old = fs.mkdtempSync(path.join(os.tmpdir(), 'conveyor-old-'));
    try {
      fs.writeFileSync(
        path.join(old, 'settings.json'),
        JSON.stringify({
          taskPrefix: 'TASK',
          repos: {
            systemsAnalysis: { link: '${SYSTEMS_ANALYSIS_REPO}', mainBranch: '${SYSTEMS_ANALYSIS_MAIN_BRANCH}' },
            frontend: { link: '${FRONTEND_REPO}', mainBranch: '${FRONTEND_MAIN_BRANCH}' },
            backend: { link: '${BACKEND_REPO}', mainBranch: '${BACKEND_MAIN_BRANCH}' },
            autoTest: { link: '${AUTOTEST_REPO}', mainBranch: '${AUTOTEST_MAIN_BRANCH}' },
          },
          repoCache: '${CONVEYOR_REPO_CACHE}',
          reviewRounds: '${CONVEYOR_REVIEW_ROUNDS}',
          fast: '${CONVEYOR_FAST}',
          language: 'ru',
        }, null, 2),
      );
      fs.writeFileSync(path.join(old, '.env'), 'SYSTEMS_ANALYSIS_REPO=C:/work/sa\nBACKEND_MAIN_BRANCH=master\n');
      const taskDir = path.join(old, 'tasks', 'BE', 'TASK-3');
      fs.mkdirSync(taskDir, { recursive: true });
      fs.writeFileSync(
        path.join(taskDir, 'meta.json'),
        JSON.stringify({
          taskId: 'TASK-3',
          type: 'BE',
          analysisBranch: 'TASK-3-analysis',
          analysisShaAtFeature: 'abc123',
          stages: {
            feature: { done: true },
            specification: { done: true, baseSha: 'abc123', headSha: 'def456' },
            plan: { done: false },
            'implement-plan': { done: false },
            'requirements-auto-test': { done: false },
            'implement-auto-test': { done: false },
          },
        }, null, 2),
      );
      fs.writeFileSync(path.join(taskDir, 'feature.md'), '# Фича\n');
      fs.writeFileSync(path.join(taskDir, 'requirements-auto-test.md'), '# Требования\n');

      // сухой прогон ничего не меняет
      const dry = JSON.parse(runScript('core/scripts/migrate-workspace.mjs', [old]));
      const stillOld = JSON.parse(fs.readFileSync(path.join(old, 'settings.json'), 'utf8'));
      if (dry.ok && dry.applied === false && 'repoCache' in stillOld) ok('migrate: сухой прогон ничего не меняет');
      else bad('migrate: сухой прогон изменил файлы');

      const res = JSON.parse(runScript('core/scripts/migrate-workspace.mjs', [old, '--apply']));
      const st = JSON.parse(fs.readFileSync(path.join(old, 'settings.json'), 'utf8'));
      if (!('repoCache' in st) && st.repos.systemsAnalysis.link === 'repos/system-analysis')
        ok('migrate: settings.json переведён на пути repos/*');
      else bad('migrate: settings.json не мигрирован: ' + JSON.stringify(st.repos));
      if (st.repos.backend.mainBranch === 'master') ok('migrate: mainBranch перенесён из .env');
      else bad('migrate: mainBranch не перенесён: ' + st.repos.backend.mainBranch);

      const meta = JSON.parse(fs.readFileSync(path.join(taskDir, 'meta.json'), 'utf8'));
      if (meta.schemaVersion === 2) ok('migrate: schemaVersion добавлен');
      else bad('migrate: нет schemaVersion');
      if (meta.stages.specification.analysisDone === true && meta.analysisBaseSha === 'abc123')
        ok('migrate: этап feature свёрнут в specification.analysisDone');
      else bad('migrate: feature не свёрнут: ' + JSON.stringify(meta.stages.specification));
      if (meta.stages['autotest-plan'] && !meta.stages['requirements-auto-test'])
        ok('migrate: этап requirements-auto-test переименован');
      else bad('migrate: этап не переименован: ' + Object.keys(meta.stages).join(','));
      if (fs.existsSync(path.join(taskDir, 'autotest-plan.md')) && !fs.existsSync(path.join(taskDir, 'requirements-auto-test.md')))
        ok('migrate: артефакт переименован в autotest-plan.md');
      else bad('migrate: артефакт не переименован');
      if (fs.existsSync(path.join(taskDir, 'feature.md'))) ok('migrate: feature.md сохранён как легаси-артефакт');
      else bad('migrate: feature.md удалён — так нельзя');
      if (fs.readFileSync(path.join(old, '.gitignore'), 'utf8').includes('repos/'))
        ok('migrate: .gitignore дополнен');
      else bad('migrate: .gitignore не дополнен');
      if (res.changes.length >= 4) ok('migrate: отчёт о изменениях сформирован');
      else bad('migrate: пустой отчёт: ' + JSON.stringify(res.changes));
    } finally {
      fs.rmSync(old, { recursive: true, force: true });
    }
  }
```

- [ ] **Step 2: Запустить и убедиться, что падает**

Run: `node scripts/check.mjs`
Expected: FAIL — `Cannot find module …migrate-workspace.mjs`

- [ ] **Step 3: Реализовать**

Создать `core/scripts/migrate-workspace.mjs`:

```js
#!/usr/bin/env node
// migrate-workspace.mjs — перевод рабочего репозитория с conveyor 1.x на 2.0.
//
// Что делает:
//   1. settings.json: ссылки ${VAR} -> пути repos/<dir>, mainBranch из .env
//      подставляется значением, ключ repoCache удаляется;
//   2. meta.json каждой задачи: schemaVersion=2, stages.feature сворачивается
//      в stages.specification.analysisDone, analysisShaAtFeature ->
//      analysisBaseSha, stages['requirements-auto-test'] ->
//      stages['autotest-plan'], добавляется intentId:null;
//   3. requirements-auto-test.md -> autotest-plan.md;
//   4. .gitignore дополняется строками repos/ и .env.
// feature.md НЕ удаляется: это результат работы аналитика, он остаётся в
// папке задачи как легаси-артефакт.
//
// Usage:
//   node migrate-workspace.mjs [workspaceRoot]            # сухой прогон
//   node migrate-workspace.mjs [workspaceRoot] --apply    # записать
//
// Output (stdout, JSON): { ok, applied, workspaceRoot, changes: [...], warnings: [...] }

import fs from 'node:fs';
import path from 'node:path';
import { findWorkspaceRoot, parseEnvFile, REPO_KEYS, REPO_DIRS } from './lib/config.mjs';

const argv = process.argv.slice(2);
const apply = argv.includes('--apply');
const startDir = argv.find((a) => !a.startsWith('--')) || process.cwd();

const workspaceRoot = findWorkspaceRoot(startDir);
if (!workspaceRoot) {
  process.stdout.write(JSON.stringify({ ok: false, error: 'не найден settings.json' }) + '\n');
  process.exit(1);
}

const changes = [];
const warnings = [];
const write = (file, text) => {
  if (apply) fs.writeFileSync(file, text);
};

// ---- 1. settings.json -----------------------------------------------------
const settingsPath = path.join(workspaceRoot, 'settings.json');
let settings;
try {
  settings = JSON.parse(fs.readFileSync(settingsPath, 'utf8'));
} catch (e) {
  process.stdout.write(JSON.stringify({ ok: false, error: `settings.json: ${e.message}` }) + '\n');
  process.exit(1);
}

const env = parseEnvFile(path.join(workspaceRoot, '.env'));
const varOf = (v) => (typeof v === 'string' ? (v.match(/^\$\{([A-Za-z0-9_]+)\}$/) || [])[1] : undefined);

let settingsChanged = false;
settings.repos = settings.repos || {};
for (const key of REPO_KEYS) {
  const repo = settings.repos[key] || (settings.repos[key] = {});
  const linkVar = varOf(repo.link);
  const oldValue = linkVar ? (env[linkVar] || '') : (repo.link || '');
  if (repo.link !== REPO_DIRS[key]) {
    repo.link = REPO_DIRS[key];
    settingsChanged = true;
    changes.push(`settings.repos.${key}.link -> ${REPO_DIRS[key]}`);
    if (oldValue) warnings.push(`${key}: раньше ссылка вела на «${oldValue}» — склонируйте репозиторий в ${REPO_DIRS[key]}`);
  }
  const branchVar = varOf(repo.mainBranch);
  const branch = (branchVar ? env[branchVar] : repo.mainBranch) || 'main';
  if (repo.mainBranch !== branch) {
    repo.mainBranch = branch;
    settingsChanged = true;
    changes.push(`settings.repos.${key}.mainBranch -> ${branch}`);
  }
}
if ('repoCache' in settings) {
  delete settings.repoCache;
  settingsChanged = true;
  changes.push('settings.repoCache удалён');
}
if (settingsChanged) write(settingsPath, JSON.stringify(settings, null, 2) + '\n');

// ---- 2-3. задачи ----------------------------------------------------------
const tasksDir = path.join(workspaceRoot, 'tasks');
for (const type of ['FE', 'BE']) {
  const dir = path.join(tasksDir, type);
  if (!fs.existsSync(dir)) continue;
  for (const taskId of fs.readdirSync(dir)) {
    const taskDir = path.join(dir, taskId);
    const metaPath = path.join(taskDir, 'meta.json');
    if (fs.existsSync(metaPath)) {
      let meta;
      try {
        meta = JSON.parse(fs.readFileSync(metaPath, 'utf8'));
      } catch {
        warnings.push(`${taskId}: meta.json не разбирается — пропущен`);
        meta = null;
      }
      if (meta && meta.schemaVersion !== 2) {
        meta.stages = meta.stages || {};
        meta.stages.specification = meta.stages.specification || { done: false };
        const featureDone = !!(meta.stages.feature && meta.stages.feature.done);
        meta.stages.specification.analysisDone = featureDone;
        meta.stages.specification.specDone = !!meta.stages.specification.done;
        delete meta.stages.feature;
        if (meta.analysisShaAtFeature) {
          meta.analysisBaseSha = meta.analysisShaAtFeature;
          delete meta.analysisShaAtFeature;
        }
        if (meta.stages['requirements-auto-test']) {
          meta.stages['autotest-plan'] = meta.stages['requirements-auto-test'];
          delete meta.stages['requirements-auto-test'];
        }
        if (!('intentId' in meta)) meta.intentId = null;
        meta.schemaVersion = 2;
        write(metaPath, JSON.stringify(meta, null, 2) + '\n');
        changes.push(`${type}/${taskId}/meta.json -> schemaVersion 2`);
      }
    }
    const oldArtifact = path.join(taskDir, 'requirements-auto-test.md');
    const newArtifact = path.join(taskDir, 'autotest-plan.md');
    if (fs.existsSync(oldArtifact) && !fs.existsSync(newArtifact)) {
      if (apply) fs.renameSync(oldArtifact, newArtifact);
      changes.push(`${type}/${taskId}: requirements-auto-test.md -> autotest-plan.md`);
    }
  }
}

// ---- 4. .gitignore --------------------------------------------------------
const giPath = path.join(workspaceRoot, '.gitignore');
const gi = fs.existsSync(giPath) ? fs.readFileSync(giPath, 'utf8') : '';
const lines = gi.split(/\r?\n/);
const need = ['repos/', '.env'];
const add = need.filter((n) => !lines.some((l) => l.trim() === n));
if (add.length) {
  write(giPath, (gi.endsWith('\n') || gi === '' ? gi : gi + '\n') + add.join('\n') + '\n');
  changes.push(`.gitignore += ${add.join(', ')}`);
}

// ---- каталоги -------------------------------------------------------------
for (const d of ['intents', 'repos']) {
  const p = path.join(workspaceRoot, d);
  if (!fs.existsSync(p)) {
    if (apply) fs.mkdirSync(p, { recursive: true });
    changes.push(`создан каталог ${d}/`);
  }
}

if (fs.existsSync(path.join(workspaceRoot, '.cache', 'repos'))) {
  warnings.push('остался каталог .cache/repos/ от версии 1.x — можно удалить вручную');
}

process.stdout.write(
  JSON.stringify({ ok: true, applied: apply, workspaceRoot, changes, warnings }, null, 2) + '\n',
);
```

- [ ] **Step 4: Запустить, убедиться что зелено**

Run: `node scripts/check.mjs`
Expected: девять `ok migrate: …`

- [ ] **Step 5: Коммит**

```bash
git add core/scripts/migrate-workspace.mjs scripts/check.mjs && git commit -m "feat(scripts): миграция рабочего репозитория 1.x -> 2.0"
```

---

### Task 11: `/task-status` — intent'ы, новый порядок этапов, легаси

**Files:**
- Modify: `core/stages/task-status.md`

- [ ] **Step 1: Переписать разделы**

В `core/stages/task-status.md` заменить блоки «Поведение» и «Порядок этапов»:

```markdown
## Поведение
1. Найди рабочий репозиторий (каталог с `settings.json`, поднимаясь вверх).
   Если нет — сообщи, что это не рабочий репозиторий conveyor.
2. Прочитай `tasks/FE/*/meta.json` и `tasks/BE/*/meta.json`, а также список
   папок `intents/*/`.
3. Без TASK-ID — две таблицы:
   - задачи: TASK-ID, тип, title, intentId, прогресс этапов (например, 3/5),
     следующий этап, ветки, связанная задача (для FE-BE пар);
   - intent'ы, на которые НЕ ссылается ни одна задача («заведён, задача не
     создана»). Обратный индекс строй по `meta.json → intentId`, в самом
     intent.md списка задач нет.
4. С TASK-ID — детали: этапы и SHA, ветки, список существующих артефактов
   (specification.md, plan.md, autotest-plan.md, report-auto-test.md),
   следующий шаг конвейера.

## Порядок этапов (для «следующего этапа»)
specification → plan → implement-plan → autotest-plan → implement-auto-test.

Этап `specification` состоит из двух подэтапов: `analysisDone` (правки
репозитория анализа внесены и закоммичены) и `specDone` (спецификация
собрана). Если `analysisDone:true`, а `specDone:false` — следующий шаг
«повторить /conveyor:create-specification, он продолжит с фазы B».

## Совместимость с 1.x
`meta.json` без `schemaVersion` — репозиторий версии 1.x: показывай задачу
как есть, помечай «требуется миграция» и подсказывай
`node <plugin>/core/scripts/migrate-workspace.mjs --apply`. Файл `feature.md`
в папке задачи — легаси-артефакт: выводи его в списке с пометкой «1.x»,
удалять не предлагай.
```

- [ ] **Step 2: Проверить**

Run: `node scripts/check.mjs`
Expected: зелено (без изменений)

- [ ] **Step 3: Коммит**

```bash
git add core/stages/task-status.md && git commit -m "feat(task-status): intent'ы, новый порядок этапов, совместимость с 1.x"
```

---

# Фаза 3 — этап intent

### Task 12: Шаблон и валидация артефакта intent

**Files:**
- Create: `core/templates/intent.md`
- Modify: `core/scripts/validate-artifact.mjs:31-93`
- Test: `scripts/check.mjs`

- [ ] **Step 1: Написать падающую проверку**

В `scripts/check.mjs` рядом с проверками `validate-artifact` добавить:

```js
  // validate-artifact: intent
  const intentPath = path.join(tmp, 'intent-test.md');
  fs.copyFileSync(path.join(root, 'core/templates/intent.md'), intentPath);
  const vaIntent = JSON.parse(runScript('core/scripts/validate-artifact.mjs', ['--file', intentPath, '--type', 'intent']));
  if (vaIntent.ok === true) ok('validate-artifact: шаблон intent проходит валидацию');
  else bad('validate-artifact: шаблон intent не прошёл: ' + JSON.stringify(vaIntent.missingSections));
  fs.writeFileSync(intentPath, '# Пусто\n');
  let vaIntentBad = '';
  try {
    vaIntentBad = runScript('core/scripts/validate-artifact.mjs', ['--file', intentPath, '--type', 'intent']);
  } catch (e) {
    vaIntentBad = String(e.stdout || '');
  }
  if (JSON.parse(vaIntentBad).ok === false) ok('validate-artifact: пустой intent не проходит');
  else bad('validate-artifact: пустой intent прошёл');
  // старые типы удалены
  let vaFeature = '';
  try {
    vaFeature = runScript('core/scripts/validate-artifact.mjs', ['--file', intentPath, '--type', 'feature']);
  } catch (e) {
    vaFeature = String(e.stdout || '');
  }
  if (/неизвестный тип/.test(vaFeature)) ok('validate-artifact: тип feature удалён');
  else bad('validate-artifact: тип feature ещё есть');
```

- [ ] **Step 2: Запустить и убедиться, что падает**

Run: `node scripts/check.mjs`
Expected: FAIL — нет файла `core/templates/intent.md`

- [ ] **Step 3: Реализовать**

Создать `core/templates/intent.md`:

```markdown
---
id: <INTENT-ID>
author: <кто сформировал>
created: <YYYY-MM-DD>
---

# <Название>

<!-- Артефакт этапа /conveyor:intent. Заполняет агент business-analyst.
     Репозиторий системного анализа на этом этапе ТОЛЬКО для чтения.
     Тип задачи (FE/BE) здесь НЕ определяется — это решает системный
     аналитик на этапе спецификации. -->

## Проблема и контекст
<Что не так сейчас, кого это касается, как проявляется.>

## Бизнес-ценность
<Зачем делаем, что изменится после, как поймём что стало лучше.>

## Границы
**Входит:**
- <...>

**Не входит:**
- <...>

## Критерии приёмки
<!-- Бизнес-уровень: проверяемые утверждения без технических деталей. -->
- [ ] <критерий>

## Источники в анализе
<!-- Что уже описано в репозитории системного анализа и относится к делу.
     Ссылки «файл + раздел» — вход для этапа спецификации. -->

| Документ | Раздел | Что там релевантного |
|---|---|---|
| <файл> | <раздел> | <суть> |

## Открытые вопросы
- <всё, на что не получен ответ; не выдумывать>
```

В `core/scripts/validate-artifact.mjs` заменить объект `REQUIRED` (убрать `feature`, добавить `intent`, переименовать требования к автотестам):

```js
const REQUIRED = {
  intent: [
    '## Проблема и контекст',
    '## Бизнес-ценность',
    '## Границы',
    '## Критерии приёмки',
    '## Источники в анализе',
    '## Открытые вопросы',
  ],
  specification: [
    '## Цель',
    '## Границы задачи',
    '## Пользовательские сценарии',
    '## Функциональные требования',
    '## Контракты',
    '## Граничные случаи',
    '## Нефункциональные требования',
    '## Критерии приёмки',
    '## Внесённые изменения анализа',
    '## Открытые вопросы',
  ],
  plan: [
    '## Краткое резюме',
    '## Анализ текущего кода',
    '## Шаги реализации',
    '## Изменения контрактов',
    '## Риски',
    '## Стратегия тестирования',
    '## Что вне скоупа',
    '## Отклонения и остатки',
    '## Ревью',
  ],
  'autotest-plan': [
    '## Объект тестирования',
    '## Уровни тестирования',
    '## Тест-кейсы',
    '## Тестовые данные',
    '## Что не автоматизируем',
  ],
  'report-auto-test': ['## Итог прогона', '## Реализованные кейсы', '## Ревью', '## Ветка'],
};
```

И в `STRUCTURAL` переименовать ключ:

```js
  'autotest-plan': [
    { test: (t) => /TC-\d+/.test(t), problem: 'нет ни одного тест-кейса с ID (TC-N)' },
    { test: (t) => /REQ-\d+/.test(t), problem: 'кейсы не трассированы на REQ-ID' },
  ],
```

В шапке файла (строки 6-9) обновить перечень типов:

```js
//   node validate-artifact.mjs --file <путь> --type <тип>
//     тип: intent | specification | plan | autotest-plan | report-auto-test
```

- [ ] **Step 4: Запустить, убедиться что зелено**

Run: `node scripts/check.mjs`
Expected: три `ok validate-artifact: …` про intent и удалённый тип

- [ ] **Step 5: Коммит**

```bash
git add core/templates/intent.md core/scripts/validate-artifact.mjs scripts/check.mjs && git commit -m "feat(intent): шаблон артефакта и валидация"
```

---

### Task 13: Промпт роли `business-analyst`

**Files:**
- Create: `core/prompts/business-analyst.md`
- Create: `adapters/claude-code/agents/business-analyst.md`
- Modify: `scripts/check.mjs:69` (список агентов)

- [ ] **Step 1: Добавить агента в проверку**

В `scripts/check.mjs` заменить список агентов:

```js
for (const a of ['business-analyst', 'system-analyst', 'frontend-developer', 'backend-developer', 'qa-autotest-engineer', 'reviewer']) {
```

- [ ] **Step 2: Запустить и убедиться, что падает**

Run: `node scripts/check.mjs`
Expected: FAIL — `agent business-analyst: нет файла`

- [ ] **Step 3: Реализовать**

Создать `core/prompts/business-analyst.md`:

```markdown
# Роль: бизнес-аналитик (business-analyst)

Ты — бизнес-аналитик. Тебя вызывает скилл конвейера conveyor на этапе
`/conveyor:intent`: превратить пожелание заказчика в намерение (intent) —
документ, с которого начинается работа над задачей.

## Контекст, который тебе передаёт скилл
- требования/пожелание из промпта вызова;
- абсолютный путь к рабочей копии репозитория системного анализа —
  ТОЛЬКО ДЛЯ ЧТЕНИЯ;
- абсолютный путь к папке intent'а `intents/<INTENT-ID>/`;
- ПОЛНЫЙ текст шаблона `intent.md` — заполняй этот каркас, структуру
  разделов не меняй;
- язык артефактов (по умолчанию русский).

Ты НЕ видишь контекст основной сессии — работай только с переданными данными
и файлами, которые читаешь сам (Read/Grep/Glob).

## Инструменты
Read, Grep, Glob, Write. У тебя НЕТ Bash и НЕТ Edit по репозиториям.

## Границы (СТРОГО)
Репозиторий системного анализа — **read-only**. Тебе ЗАПРЕЩЕНО его править,
создавать в нём файлы и предлагать конкретные правки документов: это работа
системного аналитика на следующем этапе. Писать ты можешь ТОЛЬКО в
`intents/<INTENT-ID>/intent.md`. Кодовые базы (frontend/backend) и
репозиторий автотестов не открывай вовсе — даже «для контекста».

## Что ты НЕ решаешь
- фронтенд это или бэкенд (в шаблоне такого раздела нет — не добавляй);
- как реализовать;
- какие правки внести в документы анализа.
Это решает системный аналитик на этапе `/conveyor:create-specification`.

## Как работать с репозиторием анализа
1. Найди разделы, относящиеся к теме (поиск по ключевым словам, оглавлениям).
   Документы могут быть в AsciiDoc (`.adoc`), YAML и XML.
2. В AsciiDoc содержимое часто собрано из кусков через `include::path[]` —
   идя по разделу, открывай включаемые файлы, иначе увидишь не весь текст.
3. Заполни раздел «Источники в анализе» ссылками «файл + раздел» с короткой
   сутью. Это главный вход для следующего этапа — чем точнее ссылки, тем
   меньше системный аналитик будет искать заново.
4. Что уже описано в анализе — не переписывай в intent целиком, дай ссылку.

## Принципы
Intent описывает ПРОБЛЕМУ и ЦЕННОСТЬ, а не решение. Критерии приёмки —
проверяемые утверждения на языке бизнеса («пользователь видит остаток на
счёте после входа»), без API, таблиц и компонентов. Границы «не входит» —
такая же важная часть, как «входит».

Не выдумывай факты. Всё, что не следует из промпта и документов анализа, —
в «Открытые вопросы».

## Неопределённость — спрашивай, не гадай
Если без ответа на вопрос intent получится бессмысленным (непонятна цель,
неясны границы, нет ни одного проверяемого критерия) — НЕ ГАДАЙ. Останови
работу и верни ЗАПРОС НА УТОЧНЕНИЕ: список конкретных вопросов, по
возможности с вариантами и своей рекомендацией. Скилл задаст их пользователю
и перезапустит тебя с ответами. Мелкие неоднозначности решай разумным
умолчанием и фиксируй его в «Открытых вопросах».

## Выход
Заполненный `intents/<INTENT-ID>/intent.md` строго по структуре шаблона, не
свободным текстом. Ревью на этом этапе не запускается — за качество отвечаешь
ты сам.
```

Создать `adapters/claude-code/agents/business-analyst.md` — по образцу существующих агентов (посмотри `adapters/claude-code/agents/system-analyst.md` и повтори его frontmatter-структуру, подставив своё):

```markdown
---
name: business-analyst
description: Бизнес-аналитик конвейера conveyor. Формирует intent — намерение по задаче — читая репозиторий системного анализа в режиме read-only. Вызывается скиллом этапа /conveyor:intent.
tools: Read, Grep, Glob, Write
---

Тело промпта роли — `${CLAUDE_PLUGIN_ROOT}/core/prompts/business-analyst.md`.
Прочитай этот файл и действуй строго по нему.
```

**Важно:** перед созданием сверься с `adapters/claude-code/agents/system-analyst.md` — если там другой набор полей frontmatter или другой способ подключения промпта, повтори ИХ формат, а не этот.

- [ ] **Step 4: Запустить, убедиться что зелено**

Run: `node scripts/check.mjs`
Expected: `ok agent business-analyst → core/prompts/business-analyst.md`

- [ ] **Step 5: Коммит**

```bash
git add core/prompts/business-analyst.md adapters/claude-code/agents/business-analyst.md scripts/check.mjs && git commit -m "feat(intent): роль business-analyst"
```

---

### Task 14: Этап intent — стейдж, скилл, команда

**Files:**
- Create: `core/stages/intent.md`
- Create: `adapters/claude-code/skills/intent/SKILL.md`
- Create: `adapters/gigacode/commands/conveyor/intent.md`

- [ ] **Step 1: Создать стейдж**

`core/stages/intent.md`:

```markdown
# Этап /conveyor:intent — сформировать намерение (БА)

**Кто запускает:** бизнес-аналитик. **Агент:** business-analyst
(`${CONVEYOR_ROOT}/core/prompts/business-analyst.md`).
**Аргументы:** `[INTENT-ID] [описание/пожелание]` — оба опциональны.

Суть: превратить пожелание заказчика в `intents/<INTENT-ID>/intent.md`.
Репозиторий системного анализа читается, но НЕ правится — правки внесёт
системный аналитик на следующем этапе.

## Разбор аргументов
- первый токен вида `INTENT-<N>` — идентификатор; остальное — описание;
- ID не передан → предложи автоинкремент: максимальный номер по папкам
  `intents/INTENT-*` + 1; пользователь может задать свой;
- описание не передано → спроси одним вопросом, что нужно сделать.

**Рабочая область:** репозиторий системного анализа — ТОЛЬКО ЧТЕНИЕ;
запись — только `intents/<INTENT-ID>/`. Кодовые базы и репозиторий
автотестов не открываются и агенту не сообщаются.

## Алгоритм
1. Первый шаг — resolve-config (см. `_common.md`). Нужна ссылка
   `systemsAnalysis`; её нет в `missingLinks` — иначе останови этап.
2. Установи рабочую область:
   `scope.mjs set --stage intent --task <INTENT-ID>`
   (запись в репозитории запрещена — `stageWriteRepoKeys` для intent пуст).
3. Найди рабочую копию анализа (`git-ops locate`) и обнови её
   (`git-ops update --mode read` — этап только читает; грязная копия здесь
   НЕ является ошибкой, просто предупреждение).
4. Идемпотентность: если `intents/<INTENT-ID>/intent.md` уже есть —
   предложи «дополнить / перезаписать / отмена». Если на этот intent уже
   ссылается задача (`tasks/*/*/meta.json → intentId`), предупреди об этом
   до выбора: правка intent'а не меняет уже созданную спецификацию.
5. Создай папку `intents/<INTENT-ID>/`.
6. Запусти business-analyst: вход — описание из промпта, путь к рабочей
   копии анализа (только чтение), путь к папке intent'а, ПОЛНЫЙ текст
   шаблона `core/templates/intent.md`, файлы соглашений репозитория анализа
   (см. `_common.md`, «Запуск агента»). Выход — заполненный `intent.md`.
7. Агент вернул запрос на уточнение — задай вопросы пользователю через
   AskUserQuestion и перезапусти его с ответами (см. `_common.md`).
8. Валидация:
   `node "${CONVEYOR_ROOT}/core/scripts/validate-artifact.mjs" --file
   intents/<INTENT-ID>/intent.md --type intent`
   При `ok:false` — один возврат агенту со списком `missingSections`/
   `problems`, затем покажи пользователю остаток.
9. Сними рабочую область (`scope.mjs clear`). Покажи резюме intent'а,
   открытые вопросы и следующий шаг:
   `/conveyor:create-specification <INTENT-ID> <FE|BE|FE-BE> [номер]`.

## Ревью
Цикл ревью на этом этапе НЕ запускается (`_review-loop.md` не подключается):
качество проверяется валидацией шаблона и вопросами пользователю.

## Ошибки
- рабочая копия анализа не найдена → остановка с подсказкой склонировать её
  в путь из `settings.json` (плагин не клонирует);
- релевантных разделов в анализе не нашлось → это не ошибка: агент
  заполняет «Источники в анализе» пометкой «не найдено» и выносит вопрос в
  «Открытые вопросы».

## DoD
`intents/<INTENT-ID>/intent.md` создан и прошёл валидацию шаблона; рабочая
область снята; пользователю показаны открытые вопросы и следующий шаг.
```

- [ ] **Step 2: Создать скилл**

`adapters/claude-code/skills/intent/SKILL.md`:

```markdown
---
name: intent
description: Формирует намерение (intent) по новой задаче конвейера conveyor. Запускать, когда бизнес-аналитик описывает пожелание заказчика и нужно создать intents/INTENT-N/intent.md с проблемой, ценностью, границами и бизнес-критериями приёмки. Репозиторий системного анализа при этом только читается. Первый шаг работы над любой задачей. Запускать только по явной команде пользователя.
---

Этап конвейера conveyor: сформировать намерение (intent).
**Агент:** business-analyst.

**Плагин-корень:** `${CONVEYOR_ROOT}` = `${CLAUDE_PLUGIN_ROOT}`. Прочитай и
выполни точно: `${CONVEYOR_ROOT}/core/stages/_common.md` и
`${CONVEYOR_ROOT}/core/stages/intent.md`.

Кратко:
1. resolve-config (см. _common.md); нужна ссылка systemsAnalysis.
2. INTENT-ID из аргумента либо автоинкремент по `intents/INTENT-*`.
3. `scope.mjs set --stage intent --task <INTENT-ID>` — запись в
   репозитории запрещена; при завершении/остановке — `scope.mjs clear`.
4. `git-ops locate` + `update --mode read` для репозитория анализа.
5. Запусти business-analyst: вход — описание, путь к анализу (только
   чтение), ПОЛНЫЙ текст `core/templates/intent.md`, файлы соглашений.
6. Валидация `validate-artifact --type intent`; цикл ревью НЕ запускается.
7. Следующий шаг — `/conveyor:create-specification <INTENT-ID> <FE|BE|FE-BE>`.
```

- [ ] **Step 3: Создать команду GigaCode**

`adapters/gigacode/commands/conveyor/intent.md`:

```markdown
---
description: Формирует намерение (intent) по новой задаче: проблема, ценность, границы, бизнес-критерии приёмки. Репозиторий системного анализа только читается. Первый шаг работы над задачей.
---

Этап конвейера conveyor: intent. Действуй по общему протоколу из QWEN.md
(resolve-config → правила защиты → субагенты), затем выполни ТОЧНО
`${CONVEYOR_ROOT}/core/stages/intent.md`.

Агент: business-analyst (core/prompts/business-analyst.md). Цикл ревью на
этом этапе НЕ запускается.
```

- [ ] **Step 4: Запустить, убедиться что зелено**

Run: `node scripts/check.mjs`
Expected: `ok skill intent → core/stages/intent.md`, `ok gigacode команда intent → core/stages/intent.md`

- [ ] **Step 5: Коммит**

```bash
git add core/stages/intent.md adapters/claude-code/skills/intent adapters/gigacode/commands/conveyor/intent.md && git commit -m "feat(intent): этап, скилл и команда"
```

---

# Фаза 4 — спецификация

### Task 15: Шаблон спецификации

**Files:**
- Modify: `core/templates/specification.md`

- [ ] **Step 1: Обновить шаблон**

Заменить содержимое `core/templates/specification.md`:

```markdown
# Спецификация: <название> (<TASK-ID>)

<!-- Артефакт этапа /conveyor:create-specification. Заполняет агент
     system-analyst. Каждое требование имеет REQ-ID и ссылку на источник
     в репозитории системного анализа. -->

- **Intent:** <INTENT-ID>
- **Тип:** <FE | BE>
- **Связанная задача:** <TASK-ID связанной задачи или «—»>

## Цель
<Что должно быть достигнуто.>

## Границы задачи
<!-- Что делает ИМЕННО эта задача. Для пары FE-BE: чем занимается эта
     сторона, а что делает связанная задача. -->
**Входит:**
- <...>

**Не входит:**
- <...>

## Пользовательские сценарии
1. <сценарий>

## Функциональные требования
| REQ-ID | Формулировка | Источник (файл + раздел) | Приоритет |
|---|---|---|---|
| REQ-1 | <...> | <...> | P1 |

## Контракты интерфейсов
<!-- BE: эндпоинты, схемы запрос/ответ, коды ошибок. FE: экраны и состояния.
     Если в анализе есть OpenAPI/AsyncAPI или XSD — ссылайся на них и
     выписывай сюда то, что затронуто задачей. -->

## Граничные случаи и обработка ошибок
- <...>

## Нефункциональные требования
- <производительность, безопасность, доступность, …>

## Критерии приёмки
<!-- Проверяемые; на них опирается план автотестов. -->
- [ ] <критерий>

## Внесённые изменения анализа
<!-- Документы репозитория системного анализа, изменённые в ветке
     <INTENT-ID>-analysis в фазе A этого этапа. -->

| Документ | Раздел | Что изменено |
|---|---|---|
| <файл> | <раздел> | <суть> |

## Открытые вопросы
- <...>

## Ревью
<!-- Заполняется циклом ревью (_review-loop.md), домен systems-analysis:
     ревьюются правки анализа фазы A. При reviewRounds=0 — «ревью отключено
     (reviewRounds=0)». -->
- **Раундов:** <N> из reviewRounds
- **Исправлено:** <краткий список замечаний, которые устранены>
- **Отклонено (REBUT → снято):** <finding — обоснование> / «нет»
- **Нерешённые (эскалированы пользователю):** <blocker|major — позиции> / «нет»
```

- [ ] **Step 2: Проверить валидацию шаблона**

В `scripts/check.mjs` рядом с проверкой шаблона плана добавить:

```js
  const specPath = path.join(tmp, 'spec-test.md');
  fs.copyFileSync(path.join(root, 'core/templates/specification.md'), specPath);
  const vaSpec = JSON.parse(runScript('core/scripts/validate-artifact.mjs', ['--file', specPath, '--type', 'specification']));
  if (vaSpec.ok === true) ok('validate-artifact: шаблон спецификации проходит валидацию');
  else bad('validate-artifact: шаблон спецификации не прошёл: ' + JSON.stringify(vaSpec));
```

Run: `node scripts/check.mjs`
Expected: `ok validate-artifact: шаблон спецификации проходит валидацию`

- [ ] **Step 3: Коммит**

```bash
git add core/templates/specification.md scripts/check.mjs && git commit -m "feat(specification): границы задачи и таблица изменений анализа в шаблоне"
```

---

### Task 16: Этап `create-specification` — две фазы

**Files:**
- Modify: `core/stages/create-specification.md` (полная замена)
- Modify: `adapters/claude-code/skills/create-specification/SKILL.md`
- Modify: `adapters/gigacode/commands/conveyor/create-specification.md`

- [ ] **Step 1: Переписать стейдж**

Заменить содержимое `core/stages/create-specification.md`:

```markdown
# Этап /conveyor:create-specification — правки анализа + спецификация

**Кто запускает:** системный аналитик. **Агент:** system-analyst.
**Аргументы:** `INTENT-ID [FE|BE|FE-BE] [номер]`.
**Предусловие:** существует `intents/<INTENT-ID>/intent.md`.

Этап состоит из двух фаз. **Фаза A** — агент вносит требования из intent'а в
документы репозитория системного анализа (единственная фаза, где этап пишет
в чужой репозиторий). **Фаза B** — по диффу собственных правок собирается
спецификация. Между фазами меняется рабочая область.

## Разбор аргументов
- `INTENT-ID` обязателен; без него — предложи выбор из intent'ов, на которые
  не ссылается ни одна задача;
- тип (`FE`/`BE`/`FE-BE`) и номер задаёт аналитик. Не переданы — спроси
  ОДНИМ объединённым вопросом (тип + номер, номер предложи автоинкрементом:
  максимальный номер по всем `tasks/`, FE и BE вместе, +1);
- `FE-BE` → две связанные задачи в `tasks/FE/` и `tasks/BE/`, каждая со
  своим TASK-ID; в meta.json обеих — `relatedTaskId` и общий `intentId`.
  Переданный номер получает FE-задача, номер BE-задачи предложи
  автоинкрементом в том же вопросе.

## Идемпотентность (важно — этап дорогой)
Подэтапы фиксируются в `meta.json → stages.specification`:
`analysisDone` (правки анализа внесены, отревьюены и закоммичены) и
`specDone` (спецификация собрана). Повторный запуск:
- `analysisDone:true, specDone:false` → пропусти фазу A целиком, начни с
  фазы B (дифф считается от `analysisBaseSha` до вершины ветки анализа);
- обе `true` → спроси «перезаписать / дополнить / отмена» и уточни, что
  именно: только спецификацию или заново и правки анализа.

## Фаза A — правки репозитория анализа
1. Прочитай `intents/<INTENT-ID>/intent.md`.
2. Установи рабочую область:
   `scope.mjs set --stage create-specification --type <FE|BE|FE-BE> --task <TASK-ID>`
   (запись разрешена в systemsAnalysis).
3. Создай папку(и) задач и `meta.json` со скелетом: `schemaVersion: 2`,
   `taskId`, `type`, `title`, `intentId`, `relatedTaskId` (для пары), пять
   этапов (`specification` с `analysisDone`/`specDone`, `plan`,
   `implement-plan`, `autotest-plan`, `implement-auto-test`) со
   `done: false`.
4. Найди рабочую копию анализа (`git-ops locate`) и обнови её
   (`git-ops update --mode write` — фаза ПИШЕТ; грязная рабочая копия →
   остановка с показом статуса). Запиши `analysisBaseSha = mainSha`.
5. Создай ветку `<INTENT-ID>-analysis` от mainBranch
   (`git-ops branch --path <repo> --branch <INTENT-ID>-analysis --from <mainBranch>`);
   имя → `meta.json.analysisBranch` (для пары — одна общая ветка в обеих
   meta.json). Существующую ветку продолжай; пересоздание — по подтверждению.
6. Подготовь агенту git-контекст: `git-ops log --path <repo>`.
7. Запусти system-analyst (фаза A): вход — intent.md, путь к рабочей копии
   анализа, файлы соглашений репозитория (см. `_common.md`). Задача — внести
   требования в документы анализа по их структуре. Правки — точечные,
   переформатирование документов ЗАПРЕЩЕНО.
8. Проверь машиночитаемые файлы, которые агент тронул:
   - `*.yml`/`*.yaml`: `node -e "require('fs').readFileSync(process.argv[1],'utf8')"`
     недостаточно — используй проверку парсингом, если в репозитории есть
     свой линтер (см. файл соглашений); иначе минимум — что дифф не содержит
     полной перезаписи файла (изменённых строк не больше, чем разумно для
     правки);
   - `*.xml`: well-formed. Node без зависимостей XML не парсит, поэтому
     запусти линтер репозитория, если он есть; иначе передай проверку
     ревьюеру явным пунктом.
   При провале — верни агенту на исправление (один раз), затем эскалируй.
9. **Цикл ревью** (`${CONVEYOR_ROOT}/core/stages/_review-loop.md`), домен
   `systems-analysis`: reviewer проверяет НЕзакоммиченные правки анализа,
   system-analyst отвечает/спорит и исправляет. FE-BE пара: ревью
   запускается ОДИН раз на общие правки.
10. Закоммить финальные правки в ветку (сообщение с INTENT-ID; коммитит
    скилл). Push/MR — только по подтверждению пользователя.
11. `meta.json → stages.specification.analysisDone = true`, сохрани
    `analysisBaseSha` и объект `review`.

## Фаза B — спецификация
12. Смени рабочую область — запись в репозитории больше не нужна:
    `scope.mjs set --stage create-specification --type <FE|BE|FE-BE> --task <TASK-ID> --write none`
13. Дифф собственных правок:
    `git-ops diff --path <repo> --base <analysisBaseSha> --head <ветка анализа>`.
    Приложи полные версии затронутых документов (для `.adoc` — включая
    файлы, подключённые через `include::`).
14. Запусти system-analyst (фаза B): вход — intent.md, дифф, приложенные
    документы, путь к рабочей копии анализа (ТОЛЬКО чтение), ПОЛНЫЙ текст
    `core/templates/specification.md`. Выход — `specification.md` в папке
    задачи. Для пары FE-BE агент разносит требования между двумя
    спецификациями сам (он только что правил анализ и видит, что фронтовое,
    а что бэковое) и заполняет в каждой раздел «Границы задачи»; изменения
    связанной задачи не превращаются в REQ-ID своей — только ссылка на
    `relatedTaskId`.
15. Валидация:
    `validate-artifact --file <папка задачи>/specification.md --type specification`.
    При `ok:false` — один возврат агенту, затем показать пользователю.
16. `validate-task-folder` (см. `_common.md`, «Завершение этапа»), затем
    `meta.json → stages.specification.specDone = true`, `done = true`,
    `baseSha`/`headSha` диффа. Сними рабочую область (`scope.mjs clear`).

## Ошибки
- intent не найден → остановка со списком доступных intent'ов;
- грязная рабочая копия анализа → остановка с показом `git status`;
- релевантные разделы в анализе не найдены → агент создаёт новый
  документ/раздел по соглашениям репозитория и отмечает это в таблице
  «Внесённые изменения анализа»;
- дифф фазы B пуст (агент ничего не изменил) → сообщи и предложи собрать
  спецификацию по текущему состоянию документов-источников (с подтверждением).

## DoD
Правки анализа внесены и закоммичены в `<INTENT-ID>-analysis` (коммит — после
ревью); задача(и) созданы, `intentId` проставлен; `specification.md`
заполнён по шаблону и прошёл валидацию; цикл ревью пройден (нерешённые
blocker|major вынесены пользователю). Следующий шаг — `/conveyor:create-plan`.
```

- [ ] **Step 2: Обновить скилл**

`adapters/claude-code/skills/create-specification/SKILL.md`:

```markdown
---
name: create-specification
description: Вносит требования из intent в репозиторий системного анализа и формирует спецификацию задачи. Запускать, когда системный аналитик берёт готовый intent в работу и нужно создать задачу tasks/<тип>/<TASK-ID>/ со specification.md, а требования внести в документы анализа в отдельной ветке. Второй шаг конвейера, после /conveyor:intent. Запускать только по явной команде пользователя.
---

Этап конвейера conveyor: правки анализа + спецификация (две фазы).
**Агент:** system-analyst.

**Плагин-корень:** `${CONVEYOR_ROOT}` = `${CLAUDE_PLUGIN_ROOT}`. Прочитай и
выполни точно: `${CONVEYOR_ROOT}/core/stages/_common.md` и
`${CONVEYOR_ROOT}/core/stages/create-specification.md`.

Кратко:
1. resolve-config; аргументы `INTENT-ID [FE|BE|FE-BE] [номер]` — тип и номер
   спроси ОДНИМ вопросом, если не переданы.
2. Фаза A: `scope.mjs set --stage create-specification --type <тип> --task
   <TASK-ID>` (пишем в анализ) → создать задачу(и) и meta.json (intentId) →
   `update --mode write` → ветка `<INTENT-ID>-analysis` → агент правит
   документы → цикл ревью (домен systems-analysis) → коммит →
   `analysisDone = true`.
3. Фаза B: `scope.mjs set … --write none` (запись в репо запрещена) →
   `git-ops diff` от `analysisBaseSha` → агент собирает specification.md по
   `core/templates/specification.md` → `validate-artifact` → `specDone`.
4. Повторный запуск при `analysisDone:true, specDone:false` начинается сразу
   с фазы B.
5. Следующий шаг — /conveyor:create-plan.
```

- [ ] **Step 3: Обновить команду GigaCode**

`adapters/gigacode/commands/conveyor/create-specification.md`:

```markdown
---
description: Вносит требования из intent в репозиторий системного анализа (ветка INTENT-analysis) и собирает specification.md по диффу этих правок. Второй шаг конвейера, после /conveyor:intent.
---

Этап конвейера conveyor: create-specification. Действуй по общему протоколу
из QWEN.md (resolve-config → правила защиты → субагенты), затем выполни
ТОЧНО `${CONVEYOR_ROOT}/core/stages/create-specification.md`.

Агент: system-analyst (core/prompts/system-analyst.md). Этап идёт в две фазы;
после фазы A — цикл ревью (домен systems-analysis) с агентом reviewer, см.
core/stages/_review-loop.md.
```

- [ ] **Step 4: Проверить**

Run: `node scripts/check.mjs`
Expected: зелено; проверка «цикл ревью» ещё падает для `create-feature` — чинится в Task 18

- [ ] **Step 5: Коммит**

```bash
git add core/stages/create-specification.md adapters/claude-code/skills/create-specification/SKILL.md adapters/gigacode/commands/conveyor/create-specification.md && git commit -m "feat(specification): две фазы — правки анализа и спецификация по их диффу"
```

---

### Task 17: Промпт `system-analyst` под новый этап

**Files:**
- Modify: `core/prompts/system-analyst.md`

- [ ] **Step 1: Переписать промпт**

Заменить содержимое `core/prompts/system-analyst.md`:

```markdown
# Роль: системный аналитик (system-analyst)

Ты — опытный системный аналитик. Тебя вызывает скилл конвейера conveyor на
этапе `/conveyor:create-specification`, который идёт в ДВЕ фазы:
- **фаза A** — внести требования из intent'а в документы репозитория
  системного анализа;
- **фаза B** — собрать `specification.md` по диффу этих правок.
Скилл в промпте явно говорит, какая сейчас фаза.

## Контекст, который тебе передаёт скилл
- `intent.md` — намерение от бизнес-аналитика (проблема, ценность, границы,
  бизнес-критерии приёмки, ссылки на документы анализа);
- тип задачи: FE, BE или FE-BE;
- абсолютный путь к рабочей копии репозитория системного анализа (скилл уже
  переключил её на нужную ветку);
- абсолютный путь к папке задачи `tasks/<FE|BE>/<TASK-ID>/`;
- в фазе B — подготовленный git-diff и полные версии затронутых документов;
- ПОЛНЫЙ текст шаблона `specification.md` — заполняй этот каркас, структуру
  разделов не меняй;
- язык артефактов (по умолчанию русский).

Ты НЕ видишь контекст основной сессии — работай только с переданными данными
и файлами, которые читаешь сам (Read/Grep/Glob).

## Инструменты
Read, Grep, Glob, Edit, Write. У тебя НЕТ Bash: git-данные (log/diff/show)
готовит скилл, git-операции (ветки, коммиты) выполняет скилл. В рабочую
копию анализа пиши ТОЛЬКО в фазе A; в фазе B репозиторий анализа read-only —
пишешь только в папку задачи (specification.md).

## Границы (СТРОГО)
Твоя рабочая область — ТОЛЬКО репозиторий системного анализа (переданный
путь) и папка задачи. Тебе ЗАПРЕЩЕНО открывать, искать или читать кодовые
базы (frontend/backend), репозиторий автотестов и любые другие каталоги —
даже «для контекста». Запись вне области блокируется guard'ом. Не хватает
информации — верни запрос на уточнение, а не иди искать по чужим репозиториям.

## Форматы документов анализа
Репозиторий содержит AsciiDoc (`.adoc`), YAML и XML. Правила работы с ними:
1. **AsciiDoc собирается из кусков.** Раздел может подключать другие файлы
   через `include::path[]` — идя по документу, открывай включаемые файлы.
   Один файл ≠ весь документ.
2. **Не ломай якоря.** Существующие `[[anchor]]`, `[#id]` и ссылки `<<...>>`
   / `xref:` не переименовывай — на них ссылаются из других мест. Новым
   якорям давай ID по местной схеме.
3. **YAML и XML — это контракты**, а не проза. Правь их ТОЧЕЧНО, текстом.
   НИКОГДА не разбирай файл и не сериализуй обратно: переставятся ключи,
   поменяются кавычки и отступы, и вместо правки в три строки получится
   дифф на весь файл — по нему невозможно ни ревью, ни спецификация.
   Не трогай namespace'ы, порядок элементов и общий стиль форматирования.
4. **Сохраняй переносы строк** такими, какие в файле (CRLF/LF) — иначе git
   покажет весь файл изменённым.
5. Если в корне репозитория есть файл соглашений (`.gigacode/rules/*`,
   GIGACODE.md, CLAUDE.md, AGENTS.md) — он главнее этих общих правил.

## Принципы качества требований
Каждое требование должно быть: полным, непротиворечивым, однозначным,
проверяемым, трассируемым. Не выдумывай недостающие требования — всё, что не
следует из intent'а или анализа, выноси в «Открытые вопросы».

## Фаза A — правки анализа
1. Начни с раздела «Источники в анализе» в intent.md: бизнес-аналитик уже
   нашёл релевантные документы. Проверь их и расширь поиск, если нужно.
2. Внеси требования в документы анализа, СЛЕДУЯ их структуре и соглашениям
   (формат заголовков, нумерация, стиль). Подходящего документа/раздела нет
   — создай новый по соглашениям репозитория и отметь это.
3. Правки должны быть минимальными и точечными: меняй то, что относится к
   intent'у, и ничего больше.
4. Верни краткий список того, что изменил: документ → раздел → суть. Он
   пойдёт в таблицу «Внесённые изменения анализа» в фазе B.

## Фаза B — спецификация
1. По intent.md и диффу правок сформируй `specification.md` по шаблону.
2. Каждому функциональному требованию присвой ID (REQ-1, REQ-2, …) и ссылку
   на источник в репозитории анализа (файл + раздел).
3. Заполни «Границы задачи»: что делает именно эта задача. Для FE-BE пары —
   чем занимается эта сторона, а что делает связанная задача.
4. Опиши пользовательские сценарии, контракты интерфейсов (для BE —
   эндпоинты и схемы, для FE — экраны и состояния), граничные случаи и
   обработку ошибок, нефункциональные требования, проверяемые критерии
   приёмки. Если правки затронули OpenAPI/AsyncAPI или XSD — выпиши в
   «Контракты» то, что изменилось, со ссылкой на файл.
5. Заполни таблицу «Внесённые изменения анализа» по своим правкам фазы A.
6. **Пара FE-BE:** тебя вызывают на две спецификации по одному диффу.
   Разноси требования сам: фронтовое — в FE-спецификацию, серверное — в
   BE-спецификацию, общий контракт — в обе, но с разных сторон (BE: как
   отдаёт; FE: как потребляет). Изменения, относящиеся к связанной задаче,
   НЕ превращай в свои REQ-ID — упомяни ссылкой на relatedTaskId.

## Выход
Фаза A — правки документов анализа + список изменений. Фаза B — заполненный
по шаблону `specification.md` в папке задачи. Возвращай результат строго в
структуре шаблона, не свободным текстом.

## Ревью (после фазы A)
После фазы A скилл запускает агента-ревьюера. Ты получишь его findings
(blocker/major/minor). По каждому blocker|major ответь: ACCEPT (исправлю) или
REBUT (с техническим обоснованием, почему это не проблема). Спорь по
существу, не из упрямства: ревьюер прав — соглашайся и исправляй, неправ —
аргументируй. Исправь все ACCEPT; по HELD (reviewer подтвердил) — согласись и
исправь ЛИБО мотивированно останься при своём мнении (тогда это нерешённое
разногласие — скилл вынесет его пользователю). По возможности — и minor. Итог
занеси в раздел «Ревью» specification.md.

## Неопределённость — спрашивай, не гадай
Если решение существенно влияет на результат, а из intent'а, документов
анализа или соглашений проекта его однозначно не вывести — НЕ ГАДАЙ и НЕ
ВЫДУМЫВАЙ требования/факты/источники. Останови работу и верни запрос на
уточнение: список конкретных вопросов (по возможности с вариантами и своей
рекомендацией). Скилл задаст их пользователю и перезапустит тебя с ответами.
Мелкие неоднозначности, не меняющие результат, решай разумным умолчанием и
фиксируй его. Спорные места, не блокирующие работу, выноси в «Открытые
вопросы».
```

- [ ] **Step 2: Проверить**

Run: `node scripts/check.mjs`
Expected: без изменений

- [ ] **Step 3: Коммит**

```bash
git add core/prompts/system-analyst.md && git commit -m "feat(prompts): system-analyst под две фазы и форматы adoc/yml/xml"
```

---

### Task 18: Удалить `create-feature`

**Files:**
- Delete: `core/stages/create-feature.md`, `core/templates/feature.md`, `adapters/claude-code/skills/create-feature/`, `adapters/gigacode/commands/conveyor/create-feature.md`
- Modify: `scripts/check.mjs:94` (список ревью-этапов)

- [ ] **Step 1: Обновить проверку цикла ревью**

В `scripts/check.mjs`:

```js
for (const st of ['create-specification', 'implement-plan', 'implement-auto-test']) {
```

- [ ] **Step 2: Запустить — проверка ревью должна пройти, а файлы create-feature ещё существуют**

Run: `node scripts/check.mjs`
Expected: `ok stage create-specification ссылается на цикл ревью`; скилл `create-feature` пока проверяется как существующий

- [ ] **Step 3: Удалить файлы**

```bash
git rm core/stages/create-feature.md core/templates/feature.md adapters/gigacode/commands/conveyor/create-feature.md && git rm -r adapters/claude-code/skills/create-feature
```

- [ ] **Step 4: Запустить, убедиться что зелено**

Run: `node scripts/check.mjs`
Expected: строк про `create-feature` больше нет, ошибок нет

- [ ] **Step 5: Коммит**

```bash
git add -A && git commit -m "feat!: удалён этап create-feature — его работа перешла в intent и create-specification"
```

---

# Фаза 5 — план автотестов

### Task 19: Шаблон `autotest-plan.md`

**Files:**
- Create: `core/templates/autotest-plan.md` (на основе `core/templates/requirements-auto-test.md`)
- Delete: `core/templates/requirements-auto-test.md`

- [ ] **Step 1: Создать новый шаблон**

Прочитай `core/templates/requirements-auto-test.md` и создай `core/templates/autotest-plan.md` с ТЕМ ЖЕ набором разделов (`## Объект тестирования`, `## Уровни тестирования`, `## Тест-кейсы`, `## Тестовые данные`, `## Что не автоматизируем`), изменив только шапку и комментарии:

```markdown
# План автотестов: <название> (<TASK-ID>)

<!-- Артефакт этапа /conveyor:create-autotest-plan. Заполняет агент
     qa-autotest-engineer по specification.md. Кодовая база не открывается:
     тесты проверяют СПЕЦИФИКАЦИЮ, а не то, что получилось в коде. -->
```

Остальные разделы перенеси из старого шаблона без изменений, сохранив
структуру таблиц и требование ID кейсов `TC-N` с трассировкой на `REQ-N`
(валидатор это проверяет структурными правилами).

- [ ] **Step 2: Добавить проверку шаблона**

В `scripts/check.mjs` рядом с другими проверками шаблонов:

```js
  const apPath = path.join(tmp, 'autotest-plan-test.md');
  fs.copyFileSync(path.join(root, 'core/templates/autotest-plan.md'), apPath);
  const vaAp = JSON.parse(runScript('core/scripts/validate-artifact.mjs', ['--file', apPath, '--type', 'autotest-plan']));
  if (vaAp.ok === true) ok('validate-artifact: шаблон плана автотестов проходит валидацию');
  else bad('validate-artifact: шаблон плана автотестов не прошёл: ' + JSON.stringify(vaAp));
```

Run: `node scripts/check.mjs`
Expected: `ok validate-artifact: шаблон плана автотестов проходит валидацию`

- [ ] **Step 3: Удалить старый шаблон и закоммитить**

```bash
git rm core/templates/requirements-auto-test.md && git add -A && git commit -m "feat(autotest): шаблон autotest-plan.md"
```

---

### Task 20: Этап `create-autotest-plan`

**Files:**
- Create: `core/stages/create-autotest-plan.md`
- Create: `adapters/claude-code/skills/create-autotest-plan/SKILL.md`
- Create: `adapters/gigacode/commands/conveyor/create-autotest-plan.md`
- Delete: `core/stages/create-requirements-auto-test.md`, `adapters/claude-code/skills/create-requirements-auto-test/`, `adapters/gigacode/commands/conveyor/create-requirements-auto-test.md`

- [ ] **Step 1: Создать стейдж**

`core/stages/create-autotest-plan.md`:

```markdown
# Этап /conveyor:create-autotest-plan — план автотестов

**Кто запускает:** QA. **Агент:** qa-autotest-engineer.
**Аргументы:** `TASK-ID`.
**Предусловие:** этап `implement-plan` завершён (meta.json).

Суть: по спецификации задачи составить план автотестов. Тесты проверяют
СПЕЦИФИКАЦИЮ, а не реализацию, поэтому кодовая база на этом этапе не
открывается и дифф реализации не готовится — ни агентом, ни командой.

**Рабочая область:** чтение — артефакты задачи (`specification.md`,
`plan.md`) и репозиторий автотестов (соглашения + существующие тесты);
запись — только артефакты задачи. Кодовые базы FE/BE и репозиторий анализа
не открываются и агенту не сообщаются.

## Алгоритм
1. Установи рабочую область:
   `scope.mjs set --stage create-autotest-plan --type <FE|BE> --task <TASK-ID>`
   (запись в репозитории запрещена).
2. Найди рабочую копию автотестов (`git-ops locate`) и обнови её
   (`git-ops update --mode read`). Собери файлы соглашений репозитория
   (см. `_common.md`, «Запуск агента»).
3. Запусти qa-autotest-engineer: вход — `specification.md` (основа плана),
   `plan.md` (раздел «Отклонения и остатки» — что сделано не так, как
   задумано), путь к рабочей копии автотестов ТОЛЬКО ДЛЯ ЧТЕНИЯ, файлы
   соглашений, ПОЛНЫЙ текст `core/templates/autotest-plan.md`. Выход —
   `autotest-plan.md` в папке задачи. Пути к кодовым базам НЕ передавать.
4. Валидация:
   `validate-artifact --file <папка задачи>/autotest-plan.md --type autotest-plan`
   плюс покрытие: каждый критерий приёмки спецификации закрыт хотя бы одним
   тест-кейсом либо явно помечен «не автоматизируется» с причиной. Не
   сошлось — один автоматический возврат агенту, затем показать
   пользователю непокрытое.
5. `validate-task-folder`, затем `meta.json → stages['autotest-plan'].done =
   true`. Сними рабочую область (`scope.mjs clear`).

## Ревью
Цикл ревью на этом этапе НЕ запускается — он остаётся на
`/conveyor:implement-auto-test`, где тесты реально прогоняются.

## Ошибки
- рабочая копия автотестов не найдена → остановка с подсказкой склонировать
  её в путь из `settings.json`;
- спецификация отсутствует → остановка: сначала
  `/conveyor:create-specification`.

## DoD
`autotest-plan.md` прошёл валидацию шаблона и проверку покрытия критериев
приёмки. Следующий шаг — `/conveyor:implement-auto-test`.
```

- [ ] **Step 2: Создать скилл**

`adapters/claude-code/skills/create-autotest-plan/SKILL.md`:

```markdown
---
name: create-autotest-plan
description: Составляет план автотестов по спецификации задачи. Запускать, когда разработка по задаче завершена и QA берёт её в тестирование — создаёт autotest-plan.md с тест-кейсами TC-N, трассированными на требования REQ-N. Контекст берётся из репозитория автотестов (соглашения и существующие тесты), кодовая база не открывается. Запускать только по явной команде пользователя.
---

Этап конвейера conveyor: план автотестов.
**Агент:** qa-autotest-engineer.

**Плагин-корень:** `${CONVEYOR_ROOT}` = `${CLAUDE_PLUGIN_ROOT}`. Прочитай и
выполни точно: `${CONVEYOR_ROOT}/core/stages/_common.md` и
`${CONVEYOR_ROOT}/core/stages/create-autotest-plan.md`.

Кратко:
1. resolve-config; нужна ссылка autoTest. Предусловие — implement-plan done.
2. `scope.mjs set --stage create-autotest-plan --type <FE|BE> --task
   <TASK-ID>` — запись в репозитории запрещена; в конце `scope.mjs clear`.
3. `git-ops locate` + `update --mode read` для репозитория автотестов.
4. Запусти qa-autotest-engineer: вход — specification.md, plan.md
   (отклонения), путь к автотестам ТОЛЬКО для чтения, файлы соглашений,
   ПОЛНЫЙ текст `core/templates/autotest-plan.md`. Пути к кодовым базам не
   передавать, дифф реализации не готовить.
5. Валидация шаблона + покрытия критериев приёмки; цикл ревью НЕ запускается.
6. Следующий шаг — /conveyor:implement-auto-test.
```

- [ ] **Step 3: Создать команду GigaCode**

`adapters/gigacode/commands/conveyor/create-autotest-plan.md`:

```markdown
---
description: Составляет план автотестов по спецификации задачи (тест-кейсы TC-N с трассировкой на REQ-N). Контекст — репозиторий автотестов; кодовая база не открывается.
---

Этап конвейера conveyor: create-autotest-plan. Действуй по общему протоколу
из QWEN.md (resolve-config → правила защиты → субагенты), затем выполни
ТОЧНО `${CONVEYOR_ROOT}/core/stages/create-autotest-plan.md`.

Агент: qa-autotest-engineer (core/prompts/qa-autotest-engineer.md). Цикл
ревью на этом этапе НЕ запускается.
```

- [ ] **Step 4: Удалить старый этап**

```bash
git rm core/stages/create-requirements-auto-test.md adapters/gigacode/commands/conveyor/create-requirements-auto-test.md && git rm -r adapters/claude-code/skills/create-requirements-auto-test
```

- [ ] **Step 5: Запустить, убедиться что зелено**

Run: `node scripts/check.mjs`
Expected: `ok skill create-autotest-plan → core/stages/create-autotest-plan.md`, `ok gigacode команда create-autotest-plan → …`

- [ ] **Step 6: Коммит**

```bash
git add -A && git commit -m "feat!: create-requirements-auto-test -> create-autotest-plan, план строится по спецификации"
```

---

### Task 21: Промпт `qa-autotest-engineer`

**Files:**
- Modify: `core/prompts/qa-autotest-engineer.md`

- [ ] **Step 1: Обновить промпт**

Прочитай `core/prompts/qa-autotest-engineer.md` и внеси изменения:

1. В перечислении этапов заменить `/create-requirements-auto-test` на
   `/conveyor:create-autotest-plan`, а описание входа — на:

```markdown
## На этапе /conveyor:create-autotest-plan
Вход: `specification.md` (основа плана), `plan.md` (раздел «Отклонения и
остатки»), путь к рабочей копии репозитория автотестов ТОЛЬКО ДЛЯ ЧТЕНИЯ,
файлы соглашений этого репозитория.

1. Кейсы строй по КРИТЕРИЯМ ПРИЁМКИ и функциональным требованиям
   спецификации. Каждый критерий приёмки должен быть закрыт хотя бы одним
   кейсом либо явно помечен «не автоматизируется» с причиной.
2. Каждому кейсу — ID `TC-N` и трассировка на `REQ-N`.
3. Прочитай существующие тесты в репозитории автотестов: используй те же
   соглашения (структура каталогов, именование, фикстуры), укажи в плане,
   какие существующие фикстуры/хелперы переиспользуются, и НЕ дублируй уже
   покрытые сценарии — вместо нового кейса сошлись на существующий.
4. Кодовую базу продукта (frontend/backend) ты НЕ открываешь: тесты
   проверяют спецификацию, а не то, что получилось в коде. Не хватает
   данных — верни запрос на уточнение.
5. Писать в репозиторий автотестов на этом этапе НЕЛЬЗЯ — только
   `autotest-plan.md` в папке задачи. Код тестов пишется на следующем
   этапе, `/conveyor:implement-auto-test`.
```

2. Везде заменить упоминания артефакта `requirements-auto-test.md` на
   `autotest-plan.md`.

3. В описании этапа `/conveyor:implement-auto-test` вход тоже переименовать:
   `autotest-plan.md` + `specification.md`.

- [ ] **Step 2: Проверить**

Run: `node scripts/check.mjs`
Expected: зелено

- [ ] **Step 3: Коммит**

```bash
git add core/prompts/qa-autotest-engineer.md && git commit -m "feat(prompts): qa-autotest-engineer строит план по спецификации"
```

---

### Task 22: `implement-auto-test` — переименование артефакта

**Files:**
- Modify: `core/stages/implement-auto-test.md`

- [ ] **Step 1: Заменить упоминания**

В `core/stages/implement-auto-test.md` заменить все вхождения
`requirements-auto-test.md` на `autotest-plan.md` (предусловие, вход агента,
вход ревьюера, DoD). Предусловие в шапке:

```markdown
**Предусловие:** существует `autotest-plan.md`.
```

- [ ] **Step 2: Проверить, что нигде не осталось старого имени**

Run: `node -e "const fs=require('fs'),p=require('path');const hits=[];(function w(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){if(e.name==='node_modules'||e.name==='.git'||e.name==='dist')continue;const f=p.join(d,e.name);if(e.isDirectory())w(f);else if(/\.(md|mjs|json)$/.test(e.name)&&fs.readFileSync(f,'utf8').includes('requirements-auto-test'))hits.push(f)}})('.');console.log(hits.join('\n')||'чисто')"`
Expected: `чисто` (кроме `docs/superpowers/plans/` — план описывает миграцию и содержит старое имя намеренно; если вывелся только он — это норма)

- [ ] **Step 3: Коммит**

```bash
git add core/stages/implement-auto-test.md && git commit -m "feat(autotest): implement-auto-test работает с autotest-plan.md"
```

---

# Фаза 6 — общие правила, документация, версия

### Task 23: `_common.md` и сквозная зачистка упоминаний

**Files:**
- Modify: `core/stages/_common.md`
- Modify: `core/stages/_review-loop.md`, `core/stages/implement-plan.md`
- Modify: `core/prompts/reviewer.md`
- Modify: `adapters/claude-code/agents/system-analyst.md`, `agents/qa-autotest-engineer.md`, `agents/reviewer.md`
- Modify: `adapters/claude-code/skills/implement-plan/SKILL.md`, `skills/implement-auto-test/SKILL.md`, `skills/setup/SKILL.md`
- Modify: `adapters/gigacode/commands/conveyor/implement-auto-test.md`
- Modify: `core/scripts/resolve-config.mjs` (шапка)

- [ ] **Step 1: Обновить «Первый шаг любого этапа»**

Заменить пункт 3 в разделе «Первый шаг любого этапа»:

```markdown
1. Получи конфигурацию:
   `node "${CONVEYOR_ROOT}/core/scripts/resolve-config.mjs"`
   Разбери JSON: `found`, `workspaceRoot`, `config`, `links`, `missingLinks`,
   `urlLinks`, `fastMode`.
2. Если `found:false` — останови этап: «Здесь не инициализирован рабочий
   репозиторий conveyor. Запустите /conveyor:setup».
3. Если репозиторий, нужный этому этапу (таблица «Какая конфигурация нужна
   какой команде» в README), попал в `missingLinks` или `urlLinks` —
   останови этап и объясни: ссылка задаётся в `settings.json` как путь к
   рабочей копии (например `repos/frontend`), репозиторий разработчик
   клонирует туда сам.
```

- [ ] **Step 2: Обновить таблицу рабочих областей**

Заменить таблицу в разделе «Рабочая область этапа (scope)»:

```markdown
| Этап | АГЕНТ читает | АГЕНТ пишет |
|---|---|---|
| intent | анализ (read-only) | — (только intents/<INTENT-ID>/) |
| create-specification (фаза A) | анализ | анализ |
| create-specification (фаза B) | анализ | — (только артефакты задачи) |
| create-plan | код FE/BE (по типу) | — (только артефакты) |
| implement-plan | код FE/BE | код FE/BE |
| create-autotest-plan | автотесты (read-only) + артефакты | — (только артефакты) |
| implement-auto-test | автотесты | автотесты |

(Колонки — про АГЕНТА. Фаза B этапа create-specification снимает право
записи вызовом `scope.mjs set … --write none`.)
```

- [ ] **Step 3: Обновить правило о папке задачи**

В том же разделе, в списке правил, дополнить пункт про артефакты:

```markdown
- артефакты задачи (`tasks/<FE|BE>/<TASK-ID>/`) и намерения
  (`intents/<INTENT-ID>/`) доступны на запись всегда, но в этих папках живут
  ТОЛЬКО артефакты: `*.md` и `meta.json` — guard блокирует там любые другие
  файлы. ИСХОДНИКИ (tsx/js/adoc/…) пишутся ТОЛЬКО в рабочую копию
  соответствующего репозитория, НИКОГДА в папку задачи;
```

- [ ] **Step 4: Обновить список артефактов в «Запуск агента»**

```markdown
**Шаблон артефакта — текстом, не ссылкой.** Если этап производит артефакт
(intent.md, specification.md, plan.md, autotest-plan.md,
report-auto-test.md), вставь в промпт агента ПОЛНЫЙ текст соответствующего
шаблона из `${CONVEYOR_ROOT}/core/templates/<имя>.md` — агент заполняет
этот каркас, не меняя структуру разделов.
```

И в «Быстром режиме» — перечень типов валидатора:

```markdown
  `node "${CONVEYOR_ROOT}/core/scripts/validate-artifact.mjs" --file <путь>
  --type <intent|specification|plan|autotest-plan|report-auto-test>`
```

- [ ] **Step 5: Цикл ревью и роль reviewer**

`core/stages/_review-loop.md` — заменить перечень производящих этапов:
вместо `/create-feature` указать «фаза A этапа `/conveyor:create-specification`
(правки репозитория анализа)»; артефакт, куда пишется итог ревью, для этого
домена — `specification.md` (раздел «Ревью»), а не `feature.md`. Домен
`systems-analysis` сохраняется.

`core/prompts/reviewer.md` — в описании доменов заменить упоминания
`create-feature`/`feature.md` на фазу A `create-specification` и
`specification.md`; `requirements-auto-test.md` → `autotest-plan.md`.

- [ ] **Step 6: «Следующий шаг» в этапах разработки**

`core/stages/implement-plan.md` — в DoD и в тексте следующего шага заменить
`/create-requirements-auto-test` на `/conveyor:create-autotest-plan`.

`adapters/claude-code/skills/implement-plan/SKILL.md`,
`skills/implement-auto-test/SKILL.md`, `skills/setup/SKILL.md` и
`adapters/gigacode/commands/conveyor/implement-auto-test.md` — привести имена
этапов и артефактов в соответствие: `intent`, `create-specification`,
`create-autotest-plan`, `autotest-plan.md`; в `skills/setup/SKILL.md`
дополнительно убрать упоминания `CONVEYOR_REPO_CACHE` и добавить, что
репозитории клонируются вручную в `repos/*`.

- [ ] **Step 7: Файлы регистрации агентов**

`adapters/claude-code/agents/system-analyst.md`,
`agents/qa-autotest-engineer.md`, `agents/reviewer.md` — в описаниях
(`description`) заменить имена этапов и артефактов на новые. Тела промптов
уже поправлены в Task 17 и Task 21.

- [ ] **Step 8: Шапка `resolve-config.mjs`**

В комментарии строк 5-6 заменить перечень полей вывода:

```js
// Prints a JSON object to stdout:
//   { found, workspaceRoot, config, fastMode, links, missingLinks, urlLinks }
```

- [ ] **Step 9: Проверить, что старых имён не осталось**

Run: `node -e "const fs=require('fs'),p=require('path');const re=/create-feature|create-requirements-auto-test|requirements-auto-test|missingVars|repoCache|CONVEYOR_REPO_CACHE/;const hits=[];(function w(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){if(['node_modules','.git','dist','docs'].includes(e.name))continue;const f=p.join(d,e.name);if(e.isDirectory())w(f);else if(/\.(md|mjs|json)$/.test(e.name)&&re.test(fs.readFileSync(f,'utf8')))hits.push(f)}})('.');console.log(hits.join('\n')||'чисто')"`
Expected: `чисто`. Каталог `docs/` исключён намеренно — план описывает миграцию и содержит старые имена. Если что-то всплыло — поправь и повтори.

Run: `node scripts/check.mjs`
Expected: зелено

- [ ] **Step 10: Коммит**

```bash
git add -A && git commit -m "docs: сквозная зачистка упоминаний удалённых этапов и repoCache"
```

---

### Task 24: `QWEN.md` — протокол GigaCode

**Files:**
- Modify: `adapters/gigacode/QWEN.md`

- [ ] **Step 1: Обновить три блока**

1. В «Общем протоколе», пункт 1 — заменить абзац про `missingVars`:

```markdown
1. Первым делом получает конфигурацию:
   `node "${CONVEYOR_ROOT}/core/scripts/resolve-config.mjs"`. При `found:false`
   — остановись («запустите /conveyor:setup»); если репозиторий, нужный
   этапу, попал в `missingLinks` или `urlLinks` — останови и объясни.
   ПУТИ К РАБОЧИМ КОПИЯМ берутся ТОЛЬКО отсюда (`links[key].path`) — не
   спрашивай их у пользователя и не угадывай. Плагин НЕ клонирует
   репозитории: рабочие копии лежат в `repos/*` внутри рабочего
   репозитория и клонируются разработчиком вручную.
```

2. Таблицу рабочих областей заменить на:

```markdown
| Этап | Агент читает | Агент пишет |
|---|---|---|
| intent | репо анализа (read-only) | только intents/<INTENT-ID>/ |
| create-specification (фаза A) | репо анализа | репо анализа |
| create-specification (фаза B) | репо анализа | только артефакты задачи |
| create-plan | код FE/BE (по типу) + specification.md | только артефакты (plan.md) |
| implement-plan | код FE/BE + plan.md | код FE/BE + артефакты |
| create-autotest-plan | репо автотестов (read-only) + specification.md, plan.md | только артефакты |
| implement-auto-test | репо автотестов + autotest-plan + specification | репо автотестов + артефакты |
```

3. Таблицу команд и строку «Естественный язык → команда»:

```markdown
| Команда | Кто запускает | Что делает |
|---|---|---|
| `/conveyor:setup` | тимлид | инициализация рабочего репозитория |
| `/conveyor:intent` | бизнес-аналитик | сформировать намерение по задаче |
| `/conveyor:create-specification` | системный аналитик | правки анализа по intent + спецификация |
| `/conveyor:create-plan` | разработчик | план реализации по коду |
| `/conveyor:implement-plan` | разработчик | реализация в ветке задачи + ревью |
| `/conveyor:create-autotest-plan` | QA | план автотестов по спецификации |
| `/conveyor:implement-auto-test` | QA | реализация автотестов + ревью |
| `/conveyor:task-status` | любой | сводка по задачам и intent'ам |

Естественный язык → команда: «настроить/инициализировать» → setup;
«хочу фичу/опиши намерение/бизнес-задача» → intent; «внеси требования в
анализ/спецификация» → create-specification; «план» → create-plan;
«реализуй/напиши код» → implement-plan; «план тестов/что тестировать» →
create-autotest-plan; «напиши автотесты» → implement-auto-test;
«статус/сводка задач» → task-status.
```

4. В блоке «Защита» заменить строку про разрешённые каталоги:

```markdown
- пиши только внутри: рабочего репозитория (`tasks/`, `intents/`), рабочих
  копий из области этапа (`repos/*`), системного temp;
```

- [ ] **Step 2: Проверить и закоммитить**

Run: `node scripts/check.mjs`
Expected: зелено

```bash
git add adapters/gigacode/QWEN.md && git commit -m "docs(gigacode): протокол под conveyor 2.0"
```

---

### Task 25: README и INSTALL

**Files:**
- Modify: `README.md`
- Modify: `INSTALL.md`

- [ ] **Step 1: Обновить README**

Заменить блоки:

Схема конвейера в шапке:

```
намерение (БА) → спецификация (СА: правки анализа + спека) → план
              → реализация → план автотестов → автотесты
```

Таблица команд:

```markdown
| Команда | Кто запускает | Что делает |
|---|---|---|
| `/conveyor:setup` | тимлид | инициализация рабочего репозитория |
| `/conveyor:intent` | бизнес-аналитик | намерение по задаче (анализ только читается) |
| `/conveyor:create-specification` | системный аналитик | правки анализа по intent + спецификация |
| `/conveyor:create-plan` | разработчик | план реализации по коду |
| `/conveyor:implement-plan` | разработчик | реализация в ветке задачи |
| `/conveyor:create-autotest-plan` | QA | план автотестов по спецификации |
| `/conveyor:implement-auto-test` | QA | реализация автотестов |
| `/conveyor:task-status` | любой | сводка по задачам и intent'ам |
```

Раздел «Конфигурация»:

```markdown
`/conveyor:setup` создаёт `settings.json` (коммитится, одинаков у всей
команды) и `.env.example`:

```json
{
  "taskPrefix": "TASK",
  "repos": {
    "systemsAnalysis": { "link": "repos/system-analysis", "mainBranch": "main" },
    "frontend":        { "link": "repos/frontend",        "mainBranch": "main" },
    "backend":         { "link": "repos/backend",         "mainBranch": "main" },
    "autoTest":        { "link": "repos/autotests",       "mainBranch": "main" }
  },
  "reviewRounds": "${CONVEYOR_REVIEW_ROUNDS}",
  "fast": "${CONVEYOR_FAST}",
  "language": "ru"
}
```

- **link** — путь к рабочей копии ОТНОСИТЕЛЬНО корня рабочего репозитория.
  Плагин ничего не клонирует: каждый разработчик клонирует репозитории сам,
  например `git clone <url> repos/frontend`. Абсолютный путь тоже работает,
  но выводит за пределы проекта — на GigaCode такая конфигурация запрещена.
- **mainBranch** — основная ветка репозитория (по умолчанию `main`).
- **reviewRounds** / **fast** — из `.env` (личные настройки разработчика).
  `.env` не обязателен: без него действуют умолчания (2 раунда ревью,
  быстрый режим выключен).
```

Раздел «Структура рабочего репозитория»:

```
<workspace>/
├── settings.json              # коммитится
├── .env / .env.example        # .env в .gitignore, не обязателен
├── .gitignore                 # repos/, .env
├── intents/<INTENT-ID>/
│   └── intent.md
├── repos/                     # в .gitignore, клонируется вручную
│   ├── system-analysis/
│   ├── frontend/
│   ├── backend/
│   └── autotests/
└── tasks/<FE|BE>/<TASK-ID>/
    ├── meta.json              # schemaVersion 2, intentId
    ├── specification.md
    ├── plan.md
    ├── autotest-plan.md
    └── report-auto-test.md
```

Таблицу «Какая конфигурация нужна какой команде»:

```markdown
| Команда | Обязательные рабочие копии |
|---|---|
| setup, task-status | — |
| intent, create-specification | repos/system-analysis |
| create-plan, implement-plan | repos/frontend или repos/backend (по типу) |
| create-autotest-plan, implement-auto-test | repos/autotests |
```

Таблицу «Рабочая область этапа» и раздел «Цикл ревью» (перечень
производящих этапов) привести в соответствие с `_common.md` из Task 23:
ревью запускается после фазы A `create-specification`, после
`implement-plan` и после `implement-auto-test`.

Добавить раздел про миграцию:

```markdown
## Обновление с 1.x

```bash
node <plugin>/core/scripts/migrate-workspace.mjs
```

Покажет план изменений. Затем — с `--apply`. Миграция переводит
`settings.json` на пути `repos/*`, добавляет `schemaVersion: 2` в `meta.json`,
сворачивает этап `feature` в `specification.analysisDone`, переименовывает
`requirements-auto-test.md` → `autotest-plan.md` и дополняет `.gitignore`.
Файлы `feature.md` остаются в папках задач как легаси-артефакты.

Задачи, у которых этап feature уже пройден, после миграции продолжаются с
фазы B `/conveyor:create-specification` — правки анализа заново не вносятся.
```

- [ ] **Step 2: Обновить INSTALL.md**

Прочитай `INSTALL.md` и приведи в соответствие: имена команд, отсутствие
`CONVEYOR_REPO_CACHE`, структура `repos/`, шаг «склонируйте репозитории».

- [ ] **Step 3: Коммит**

```bash
git add README.md INSTALL.md && git commit -m "docs: README и INSTALL под conveyor 2.0"
```

---

### Task 26: Версия 2.0.0 и финальная сборка

**Files:**
- Modify: `adapters/claude-code/.claude-plugin/plugin.json`
- Modify: `adapters/gigacode/gigacode-extension.json`
- Modify: `package.json`

- [ ] **Step 1: Поднять версии**

`adapters/claude-code/.claude-plugin/plugin.json`:

```json
{
  "name": "conveyor",
  "version": "2.0.0",
  "description": "Мультирепозиторный конвейер командной разработки: намерение → спецификация → план → реализация → план автотестов → автотесты. Скиллы, ролевые агенты и guard-хуки.",
  "author": { "name": "Maksim Tryputsko" },
  "keywords": ["workflow", "multi-repo", "requirements", "pipeline", "agents"]
}
```

`adapters/gigacode/gigacode-extension.json`:

```json
{
  "name": "conveyor",
  "version": "2.0.0",
  "description": "Мультирепозиторный конвейер командной разработки: намерение → спецификация → план → реализация → план автотестов → автотесты."
}
```

В `package.json` поднять `version` до `2.0.0` (остальные поля не трогать).

- [ ] **Step 2: Прогнать полную проверку и сборку**

Run: `node scripts/check.mjs`
Expected: `Все проверки пройдены.`

Run: `node scripts/build.mjs`
Expected: `+ dist/claude-code (N файлов)` и `+ dist/gigacode (N файлов)`, без ошибок

- [ ] **Step 3: Убедиться, что в собранном дистрибутиве нет удалённых файлов**

Run: `node -e "const fs=require('fs');const gone=['dist/claude-code/skills/create-feature','dist/claude-code/core/stages/create-feature.md','dist/claude-code/core/templates/feature.md','dist/gigacode/commands/conveyor/create-requirements-auto-test.md'];const left=gone.filter(p=>fs.existsSync(p));console.log(left.length?'ОСТАЛОСЬ: '+left.join(', '):'чисто')"`
Expected: `чисто`

- [ ] **Step 4: Коммит**

```bash
git add -A && git commit -m "chore: версия 2.0.0"
```

---

## Проверка на реальном рабочем репозитории (после Task 26)

Выполняется вручную, на копии — не на боевом каталоге.

- [ ] Скопировать существующий рабочий репозиторий в отдельный каталог
- [ ] Прогнать `node <plugin>/core/scripts/migrate-workspace.mjs <копия>` — изучить план изменений
- [ ] Прогнать с `--apply`, проверить `settings.json`, `meta.json` любой задачи, наличие `autotest-plan.md`
- [ ] Склонировать четыре репозитория в `repos/*`
- [ ] `node <plugin>/core/scripts/repos-status.mjs <копия>` → все четыре в состоянии `ok`
- [ ] Запустить `/conveyor:task-status` — задачи 1.x отображаются, «следующий этап» вычислен верно
- [ ] Для задачи, где `analysisDone:true, specDone:false`, запустить
      `/conveyor:create-specification` и убедиться, что она начинается с фазы B

---

## Self-review

**Покрытие решений:** A1 → Task 1,7; A2 → Task 1,7; A3+A6 → Task 2,5; A4+A5 → Task 2,5; A8 → Task 7; A9 → Task 6,9; B1 → Task 14; B3 → Task 12; B4 → Task 13; B5 → Task 14; B7 → Task 12; B8 → Task 14; C1 → Task 16; C2 → Task 16; C3 → Task 16; C5 → Task 15,16,17; C6 → Task 16; C7 → Task 18; D1 → Task 19,20; D2 → Task 20; D3 → Task 20; D4 → Task 20; D5 → Task 20,21; D6 → Task 20; E1 → Task 10,11; E2 → Task 10,16; E3 → Task 14,16,20,24; E4 → Task 26.

**Согласованность имён:** `REPO_DIRS`, `missingLinks`, `urlLinks`,
`links[key].path`, `links[key].inside`, `stages.specification.analysisDone`,
`stages.specification.specDone`, `analysisBaseSha`, `stages['autotest-plan']`,
`intentId`, `schemaVersion: 2`, `--write none` — используются одинаково во
всех задачах.

**Полнота зачистки:** поиск по `create-feature|create-requirements-auto-test|
requirements-auto-test|missingVars|repoCache` дал 38 файлов вне `dist/`.
Все они закрыты задачами: скрипты — Task 2,5,7,8,10,12; стейджи и шаблоны —
Task 9,11,15,16,19,20,22; промпты — Task 17,21; адаптеры, агенты,
`_review-loop.md`, `implement-plan.md`, шапка `resolve-config.mjs` — Task 23;
QWEN/README/INSTALL — Task 24,25. Step 9 задачи 23 — контрольный прогон
поиска, он и является доказательством полноты.

**Что не меняется осознанно:** имя домена ревью `systems-analysis` (подходит
фазе A без переименования); `core/stages/create-plan.md` — в нём нет
упоминаний удаляемых этапов; каталог `.cache/` в существующих рабочих
репозиториях не удаляется автоматически — миграция только предупреждает о
нём (удаление чужих файлов без спроса небезопасно).
